= BACKLOG — academy-gradle
:toc:
:toclevels: 2

_Dernière mise à jour_ : 2026-09-25 — **ACADEMY-11-4 TERMINÉ + EPIC ACADEMY-11 CLOS (S-016)** — service compose one-shot `moodle-material` : il **applique** le plan stagé après le schéma Moodle, en pilotant les outils natifs de l'image (`moosh`). **Volume nommé `moodle-html:/var/www/html`** partagé par `moodle` et `moodle-material` (indispensable : `moosh` bootstrappe contre l'install). **Garde de rejeu** `sha256(plan.json)` → marqueur `.academy-applied-plan` dans le volume inscriptible (`moosh activity-add` **ne dédoublonne pas**). **Input tolérant** `GenerateInstallerTask.moodleMaterialFiles` (`@InputFiles @Optional`) : un re-run simple ré-rend un plan changé (fin de la staleness silencieuse). Lecture matériel partagée (`MoodleMaterialReader`). **Dogfooding réel `compose up`** : cours `formation-fpa` + sections + activités, contenu AsciiDoc en base, rejeu no-op — **3 bugs réels attrapés**. Gates 710 unit + 21 functional + 71 Cucumber 0 FAIL. **Ordre du jour pilote : ACADEMY-12-3** (dogfooding Windows réel, hôte requis), puis ACADEMY-9/ACADEMY-10 (cadrage/décision pilote).

'''
== Vision actée (S-002, arbitrage pilote)

Le borough Bagdad (academy-gradle) livre une **ressource autonome** qui contient tout ce dont l'apprenant a besoin pour expérimenter la formation du pipeline cccp.education :

```
┌─────────────────────────────  HOST  ─────────────────────────────┐
│  Docker        (socle conteneurs)                                │
│  Java Temurin 25 + Gradle 9.7.1  →  lance les plugins            │
│      education.cccp (bakery, document, slider, capsule, ...)     │
└──────────────────────────────┬───────────────────────────────────┘
                               │ docker compose
┌───────────────────────────── ▼  DOCKER  ─────────────────────────┐
│  Moodle 4.x  (LMS formation, cours seedé)                        │
│  PostgreSQL  (base Moodle, volumes persistants)  ← pilote 09-22  │
│  Ollama      (runtime LLM, modèles, port 11434)                  │
│  Portainer   (GUI Docker, port 9000 — remplace Docker Desktop)   │
│  workspace   (session zsh + opencode via conteneur)              │
└──────────────────────────────────────────────────────────────────┘
```

- L'installeur par plateforme (sh / bat) provisionne l'hôte, télécharge les dépendances, génère le `docker-compose.yml`, seede la base Moodle et les volumes, et génère `opencode.json` exposant les tâches Gradle de l'écosystème.
- `opencode` fonctionne hôte OU conteneur : il déclenche les tâches Gradle `education.cccp` sur le projet de l'apprenant (les plugins sont consommés depuis Maven Central + Gradle, zéro VPS).
- Rôle produit : **distribution** de la machine d'expérimentation (ne réplique pas KINSHASA — provisionnement dev ; KINSHASA reste N0).

'''
== Épics

|===
| Épic | Description | Statut
| ACADEMY-0 | Bootstrap academy-gradle (création des fichiers de gouvernance, structure du plugin, placeholder de code) | ✅ TERMINÉ (S-000)
| ACADEMY-0-BIS | Mise en conformité architecture article 0124 + fusion gouvernance + licence Apache 2.0 + repo Git | ✅ TERMINÉ (S-001)
| ACADEMY-1 | Socle du plugin générateur d'installateurs (extension, tâches de génération, agrégateur) | ✅ TERMINÉ (S-003)
| ACADEMY-6 | Installeur hybride multiplateforme (Docker + compose Moodle/PostgreSQL/Ollama/Portainer/workspace opencode zsh : network, volumes nommés, healthcheck PostgreSQL, `.env`, DB seedée data-only, images réelles, hôte = Docker uniquement) | ✅ TERMINÉ (S-008) — 6-0 S-003, 6-4 S-005 (corrigé S-007), 6-5 S-006, 6-6 S-007, 6-7 S-008 ; 6-1/2/3 ✅ CADUQUES S-008
| ACADEMY-5 | Intégration du contrat InteractionProtocol (exposition de opencode.json + session zsh opencode lançant les tâches Gradle de l'écosystème) | ✅ TERMINÉ (S-009) — 5-0 cadrage, 5-1 domaine+wiring+dogfooding ; 5-2 absorbée
| ACADEMY-7 | Intégration du BYOK LLM (configuration du fournisseur LLM : OLLAMA_LOCAL, OLLAMA_CLOUD, GEMINI, HUGGINGFACE, CUSTOM) | ✅ TERMINÉ (S-010) — 7-0 cadrage, 7-1 domaine `academy.byok` + générateur multi-provider, 7-2 wiring + chaîne `.env`→compose→`{env:NAME}`, 7-3 BDD + dogfooding réel
| ACADEMY-4 | Consommation du matériel versionné — lecture du contrat N0 `MaterialUpdateContract` + vérification du pivot EPIC K (le transport git appartient au bureau) | ✅ TERMINÉ (S-012) — 4-0 cadrage (frontière des verbes corrigée), 4-1 domaine `academy.material`, 4-2 tâche `inspectTrainingMaterial` + scaffold `MATERIAL.md`, 4-3 BDD + dogfooding ; US « seed Moodle » différée (D-ACADEMY-4-12)
| ACADEMY-2 | Intégration du contrat FormationRuntimePort (boucle d'assistance startSession/executeTurn/endSession) | ✅ CLOS (S-013) — academy **ne porte pas** la boucle runtime (D-ACADEMY-11-1) : l'orchestration agentique est l'étage **privé** (workspace-gradle/edster). `ToolExposure` (N0) est en revanche **branché** (`OpenCodeCatalog.exposure()`)
| ACADEMY-3 | Intégration du contrat SessionMemoryContract (suivi apprenant : progression, comprehension, weakPoints, annotations) | ✅ CLOS (S-013) — implémenté hors academy par `PgVectorSessionMemory` (codex N2, S-094/CDX-RC-04) ; le TODO « zéro implémenteur » était **périmé**
| ACADEMY-8 | Webhooks Moodle + bridge opencode (avec Ktor, conformément à la décision D4 du VISION_PIPELINE_FORMATION.adoc) | ✅ TERMINÉ (S-011) — 8-0 cadrage, 8-1 domaine `academy.bridge` + serveur Ktor, 8-2 tâche opt-in + scaffold, 8-3 BDD + dogfooding
| ACADEMY-9 | Support Qualiopi (bilan de session, traces, conformité aux critères) | 🟡 À FAIRE
| ACADEMY-10 | Migration du bridge Ktor → Spring Boot WebFlux + coroutines Kotlin + R2DBC + PostgreSQL (Docker) + Jackson 3 | 🟡 À FAIRE (cadré S-012)
| ACADEMY-11 | Injection du matériel pédagogique dans Moodle (data structurée AsciiDoc → plan + applicateur natif) | ✅ TERMINÉ 4/4 US (S-016) — 11-0/11-1/11-2/11-3 ✅ S-013 ; 11-4 ✅ S-016 (service compose one-shot `moodle-material` + dogfooding `compose up`)
| ACADEMY-12 | Provisionnement de l'engine conteneurs par OS (Linux natif / Windows Docker Desktop WSL2 / macOS Colima) + Portainer partout | 🟡 EN COURS 3/4 US (S-015) — 12-0/12-1/12-2 ✅ ; 12-3 🟡 À FAIRE
|===

'''
== Sections détaillées des Épics

=== ACADEMY-0-BIS : Mise en conformité architecture article 0124 + gouvernance + repo

*Statut* : ✅ TERMINÉ (S-001)

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-0-BIS-0 | Fusion gouvernance racine → academy-plugin/.agents (git mv, 100% rename)
| ACADEMY-0-BIS-1 | Alignement article 0124 : .gitignore, wrappers 9.7.1, catalogues séparés, builds racine/plugin, signing, POM Apache 2.0
| ACADEMY-0-BIS-2 | Licence Apache 2.0 + vérification repo GitHub cccp-education/academy-gradle
|===

=== ACADEMY-1 : Socle du plugin générateur d'installateurs

*Statut* : ✅ TERMINÉ (S-003)

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-1-0 | Extension typée `academyInstaller` (applicationName, applicationVersion, javaVersion, gradleVersion, targets, compose options, credentials via env) — pattern Property-based ✅
| ACADEMY-1-1 | Tâches `generateLinuxInstaller` / `generateWindowsInstaller` / `generateMacInstaller` / `buildAllInstallers` (agrégat) — registration + dépendances ✅
| ACADEMY-1-2 | Tests unitaires : chargement du plugin, défauts d'extension, aggregation correcte ✅
|===

=== ACADEMY-6 : Installeur hybride multiplateforme (machine d'expérimentation)

*Statut* : ✅ TERMINÉ (S-008) — ACADEMY-6-0 (cadrage) ✅ S-003 — doc `CADRAGE_EPIC_ACADEMY_INSTALLER.adoc` (constats, décisions D-ACADEMY-6-1..17, options écartées, articulation 1→6→5→7) ; **ACADEMY-6-4 (scaffold compose complet) ✅ S-005, corrigé S-007** (images réelles) ; **ACADEMY-6-5 (seed DB data-only) ✅ S-006** ; **ACADEMY-6-6 (dogfooding bout-en-bout) ✅ S-007** ; **ACADEMY-6-7 (BDD étendu + hôte Docker-only) ✅ S-008** ; **ACADEMY-6-1/2/3 ✅ CADUQUES S-008** (absorbés par 6-7).

*Orientation actée (S-002)* : la ressource contient TOUT pour expérimenter la formation :
- Hôte : Docker uniquement (D-ACADEMY-6-10 — le toolchain Java/Gradle vit dans l'image workspace).
- Docker : Moodle + PostgreSQL (volumes nommés + base seedée) + Ollama (runtime LLM) + Portainer (GUI Docker, remplace Docker Desktop) + conteneur workspace zsh+opencode.
- opencode (hôte ou conteneur) déclenche les tâches Gradle de l'écosystème sur le projet apprenant.

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-6-0 | Cadrage architecture (gouvernance pure) : frontière installateur/KINSHASA, contenu des scripts, seed DB, articulation ACADEMY-1/5/6/7, doc CADRAGE_EPIC_ACADEMY_INSTALLER.adoc ✅ S-003
| ACADEMY-6-1 | Générateur d'installateur Linux (`.sh`) : bootstrap Docker, téléchargement Java Temurin 25, Gradle 9.7.1, PATH/JAVA_HOME/alternatives, garde idempotence — **✅ CADUQUE S-008** (D-ACADEMY-6-10 : hôte = Docker only ; provisioning hôte retiré, absorbé par 6-7)
| ACADEMY-6-2 | Générateur d'installateur Windows (`.bat`) : vérif admin, PowerShell downloads, variables d'environnement, profil 64-bit — **✅ CADUQUE S-008** (D-ACADEMY-6-10)
| ACADEMY-6-3 | Générateur d'installateur macOS (`.sh`) : Homebrew/cask Docker Desktop, détection arch, Java/Gradle via brew, sans sudo — **✅ CADUQUE S-008** (D-ACADEMY-6-10)
| ACADEMY-6-4 | Génération `docker-compose.yml` : services Moodle + **PostgreSQL** (base Moodle, `POSTGRES_PASSWORD`/`POSTGRES_DB`, volumes persistants, D-ACADEMY-6-13) + workspace opencode zsh + **Ollama** (image `ollama/ollama`, volume modèles, port `11434`, D-ACADEMY-6-11) + **Portainer** (image `portainer/portainer`, port `9000`, volume données, mount `/var/run/docker.sock` — surface GUI remplaçant Docker Desktop, D-ACADEMY-6-12), **network dédié `academy-net`, volumes nommés `postgres-data`/`ollama-models`/`portainer-data`, healthcheck PostgreSQL `pg_isready` + `depends_on: service_healthy`, Moodle câblé `pgsql`, fichier `.env` généré (noms seulement, valeurs runtime)** — parité **structurelle** Linux/macOS + Windows (`composeBody()`/`envBody()` source unique partagée par le heredoc bash et le writer batch). ✅ **TERMINÉ S-005** (dogfooding `docker compose config` VALIDE)
| ACADEMY-6-5 | Seed de la base Moodle + configuration volumes au premier `compose up` — service one-shot `moodle-seed` (image `postgres:16`, `restart: "no"`, `entrypoint sh /seed/seed.sh`), entrypoint idempotent (poll `mdl_course` puis `psql -f /seed/seed-course.sql`), SQL data-only (`INSERT ... WHERE NOT EXISTS` sur `idnumber='academy-seed'`), staging `$APP_DIR/seed/`, parité structurelle Linux/Windows (`seedScriptBody()`/`seedCourseSqlBody()` + `escapeCmd()`). ✅ **TERMINÉ S-006** (idempotence prouvée PostgreSQL 16 réel)
| ACADEMY-6-6 | Dogfooding : build réel de l'installeur Linux sur un conteneur éphémère, validation des étapes 1→7 — ✅ **TERMINÉ S-007** (run réel complet : PostgreSQL/Ollama/Moodle healthy, seed `INSERT 0 1`→`INSERT 0 0`, Moodle 5.2.3 HTTP 200 ; 5 écarts de scaffold inexécutable découverts + corrigés D-ACADEMY-6-14..17)
| ACADEMY-6-7 | Cucumber BDD : scénarios installeur étendus (contenu OS, compose, idempotence, erreurs, opencode.json) — ✅ **TERMINÉ S-008** : +6 scénarios BDD, +7 unit, +1 TestKit ; alignement du contenu OS sur D-ACADEMY-6-10 (hôte = Docker only) ; RED prouvé aux 3 niveaux, GREEN 28 unit + 6 functional + 21 scénarios
|===

=== ACADEMY-5 : Intégration du contrat InteractionProtocol (opencode)

*Statut* : ✅ TERMINÉ (S-009) — ACADEMY-5-0 (cadrage `CADRAGE_EPIC_ACADEMY_5_OPENCODE.adoc`, D-ACADEMY-5-1..11) ✅ S-009 ; **ACADEMY-5-1 (domaine + wiring + dogfooding) ✅ S-009** ; **ACADEMY-5-2 ✅ ABSORBÉE S-009** (le `project/` généré = projet consommateur).

*Livré* : domaine pur `education.cccp.academy.opencode` (`OpenCodeConfig`, `OpenCodePlugin`, `OpenCodeCatalog` — 11 plugins publics catalogués, tâches vérifiées source S-009 —, `OpenCodeConfigGenerator` JSON zéro-lib, `LearnerGuideGenerator` `AGENTS.md`, `WorkspaceDockerfileGenerator` base `gradle:9.7.1-jdk25`) ; service `workspace` dans `composeBody()` (`build: ./project`, profile `agent` opt-in, TODO S-007 résolu) ; `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` générés (parité structurelle Linux/Windows) ; extension +3 properties (`openCodeEnabled`/`openCodeModel`/`openCodeProviderUrl`).

*Dogfooding (preuve réelle)* : installeur Linux exécuté dans un HOME isolé → `sh -n` + `docker compose config` + `jq` VALIDES, `docker build` du Dockerfile généré SUCCÈS, `opencode 1.18.31` en tant que `learner`. Bug attrapé (D-ACADEMY-5-11) : install opencode en root → `/root/.opencode` invisible au learner, corrigé (`USER learner` avant install).

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-5-0 | Cadrage InteractionProtocol — articulation opencode.json ↔ tâches Gradle education.cccp, service workspace (image générée) ✅ S-009
| ACADEMY-5-1 | Exposition des tâches Gradle publiques + `opencode.json` + `AGENTS.md` + image workspace générée (profile `agent`) ✅ S-009
| ACADEMY-5-2 | Provision du projet consommateur `education.cccp` — **✅ ABSORBÉE S-009** (le `project/` généré : Dockerfile + opencode.json + AGENTS.md + README)
|===

*Note méthode* : l'implémentation des contrats N0 `FormationRuntimePort`/`InteractionProtocol` (D-ACADEMY-5-8) reste séquencée en ACADEMY-2/3 — elle exige un runtime LLM (BYOK, ✅ ACADEMY-7) et un pont Gradle↔opencode (ACADEMY-8 Ktor). ACADEMY-5 livre l'*exposition*, pas la boucle de session.

=== ACADEMY-7 : Intégration du BYOK LLM (multi-provider)

*Statut* : ✅ TERMINÉ (S-010) — ACADEMY-7-0 (cadrage `CADRAGE_EPIC_ACADEMY_7_BYOK.adoc`, D-ACADEMY-7-1..10, mapping provider vérifié S-010) ✅ ; **ACADEMY-7-1 (domaine `academy.byok` + générateur multi-provider) ✅ S-010** ; **ACADEMY-7-2 (wiring + chaîne BYOK) ✅ S-010** ; **ACADEMY-7-3 (BDD + dogfooding) ✅ S-010**.

*Livré* : domaine pur `education.cccp.academy.byok` (`ByokProviderSpec` + `ByokProviderCatalog`, mapping total `LlmProviderKind` → provider opencode réel : `ollama`, `ollama-cloud` `https://ollama.com/v1`/`OLLAMA_API_KEY`, `google` npm `@ai-sdk/google`/`GEMINI_API_KEY`, `huggingface` `https://router.huggingface.co/v1`/`HF_TOKEN`, `custom`) ; `OpenCodeConfig` +`provider`/`apiKeyEnvVar` (défaut byte-identique ACADEMY-5) ; `OpenCodeConfigGenerator` multi-provider (`apiKey` toujours `{env:NAME}`, interpolation officielle opencode — jamais la valeur) ; `LearnerGuideGenerator` section BYOK concrète ; extension +2 properties (`openCodeProvider` défaut `OLLAMA_LOCAL`, `openCodeApiKeyEnvVar`) ; `.env` documente le nom (valeur vide) et `composeBody()` transmet la variable au service `workspace`. Dépendance N0 `runtime-contracts:0.0.1` (**supersede D-ACADEMY-5-9**).

*Dogfooding (preuve réelle)* : consommateur transitoire /tmp, installeur exécuté en HOME isolé — provider GEMINI : `sh -n` + `docker compose config` + `jq` VALIDES, `.env` `GEMINI_API_KEY=` (vide), compose transmet `GEMINI_API_KEY: ${GEMINI_API_KEY}` ; **preuve de non-fuite** : avec `dummy-not-a-real-key`, la valeur est dans le `compose config` et **absente du `opencode.json`** ; défaut OLLAMA_LOCAL : `opencode.json` **byte-identique** ACADEMY-5.

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-7-0 | Cadrage BYOK — provider N0 → provider opencode, clé par nom d'env var, options écartées ✅ S-010
| ACADEMY-7-1 | Domaine `academy.byok` (`ByokProviderSpec`/`ByokProviderCatalog`) + `OpenCodeConfig` +2 champs + `OpenCodeConfigGenerator` multi-provider + `LearnerGuideGenerator` BYOK + dépendance N0 ✅ S-010
| ACADEMY-7-2 | Extension +2 properties + `InstallerPlatform`/`GenerateInstallerTask` + `.env` nom-seul + compose transmet la variable + parité Linux/Windows ✅ S-010
| ACADEMY-7-3 | BDD `academy_byok.feature` `@byok` 8 scénarios + runner dédié + dogfooding réel (clé absente du JSON) ✅ S-010
|===

*Note* : `poolPorts` (N0) n'est pas rendu dans `opencode.json` — c'est une concern de rotation runtime (codebase N1 `LlmProviderResolver`) ; l'agent parle au endpoint cloud officiel (D-ACADEMY-7-9).

=== ACADEMY-8 : Webhooks Moodle + bridge Ktor

*Statut* : ✅ TERMINÉ (S-011) — ACADEMY-8-0 (cadrage `CADRAGE_EPIC_ACADEMY_8_KTOR.adoc`, D-ACADEMY-8-1..10) ✅ ; **ACADEMY-8-1 (domaine `academy.bridge` + serveur Ktor) ✅ S-011** ; **ACADEMY-8-2 (tâche opt-in + scaffold) ✅ S-011** ; **ACADEMY-8-3 (BDD + dogfooding) ✅ S-011**.

*Livré* : domaine pur `education.cccp.academy.bridge` (`MoodleEvent` — schéma **academy** à payload opaque, jamais un format Moodle tiers ; `BridgeEventTranslator` → `SessionPrompt` N0 à session id **stable** via `UUID.nameUUIDFromBytes`, rejouable ; `BridgeRoutes` table unique `/health`+`/events/moodle`+`/session` ; `BridgeServerConfig` ; `BridgeWire` **anti-corruption** — les contrats N0 ne sont pas `@Serializable` ; `ByokBridgeSupport`) + serveur `WebhookBridgeServer` (`installBridge` module unique test+prod, `startBridge` Netty daemon, acks, 400/404) + tâche `serveWebhookBridge` **opt-in** (`@DisableCachingByDefault`, `onlyIf { bridgeEnabled }`, seam `serverLauncher`) + extension +3 properties (`bridgeEnabled` défaut `false`, `bridgeHost` `127.0.0.1`, `bridgePort` `8765`) + `AGENTS.md` section bridge **conditionnelle** (`BridgeGuide`). Le bridge **route et acquitte — il ne pense pas** (zéro LLM).

*Dogfooding (preuve réelle)* : consommateur transitoire /tmp, serveur lancé par la tâche Gradle port 8791, `curl` réel — `GET /health` 200 `{"status":"up","provider":"ollama","model":"gpt-oss:120b-cloud"}`, `POST /events/moodle` ack `COMPLETED` session stable, `POST /session` ack, JSON invalide **400**, route inconnue **404**, arrêt propre.

*Note de gouvernance* : la justification de la vision § 8 D4 (« le bridge opencode existe déjà ») était **inexacte** — le bridge codebase est stdin/stdout, pas HTTP. Ktor est acté par décision pilote mais documenté comme **transport additionnel** (D-ACADEMY-8-1), et le daemon N1 n'est pas réutilisé (academy ne consomme pas les tâches N1).

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-8-0 | Cadrage bridge Ktor — transport additionnel, schéma academy, options écartées ✅ S-011
| ACADEMY-8-1 | Domaine `academy.bridge` (`MoodleEvent`/`BridgeEventTranslator`/`BridgeRoutes`/`BridgeServerConfig`/`BridgeWire`/`ByokBridgeSupport`) + serveur `WebhookBridgeServer` Ktor ✅ S-011
| ACADEMY-8-2 | Extension +3 properties + tâche `serveWebhookBridge` opt-in + `AGENTS.md` conditionnel ✅ S-011
| ACADEMY-8-3 | BDD `academy_bridge.feature` `@bridge` 6 scénarios + runner dédié + dogfooding réel serveur/curl ✅ S-011
|===

=== ACADEMY-4 : Consommation du matériel versionné (MaterialUpdateContract)

*Statut* : ✅ TERMINÉ (S-012) — ACADEMY-4-0 (cadrage `CADRAGE_EPIC_ACADEMY_4_MATERIAL.adoc`, D-ACADEMY-4-1..12) ✅ ; **ACADEMY-4-1 (domaine `academy.material`) ✅ S-012** ; **ACADEMY-4-2 (wiring + tâche + scaffold) ✅ S-012** ; **ACADEMY-4-3 (BDD + dogfooding) ✅ S-012**.

*Correction du cadrage* : la définition d'origine (« implémenter l'interface `MaterialUpdateContract` ; recevoir le matériel ; alimenter le seed Moodle ») était **inexacte sur trois points**, corrigée par l'audit S-012 :

. `MaterialUpdateContract` est une **data class** ; l'interface est `MaterialUpdateResolver`.
. Le **transport git appartient au bureau** (vision § 3.2 : « bureau l'implémente, academy le lit ») — academy n'exécute jamais git (`BUREAU-3`, 🔴 À FAIRE).
. Alimenter le seed Moodle exige le contrat SPG/SPD canonique (MVP0 training non fait) → **US différée** (D-ACADEMY-4-12).

*Livré* : domaine pur `education.cccp.academy.material` (`MaterialArtifactType` — 6 livrables, `fromPivotType` ne devine jamais ; `MaterialRequirement` — lecture du N0 ; `MaterialManifest` — lecture du **pivot EPIC K** `metadata.json`, tolérante (`ignoreUnknownKeys`, JSON malformé → `null`) ; `MaterialReadiness` sealed `Ready`/`Incomplete`/`Empty` ; `MaterialInspector` — verdict pur, **jamais d'exception sur matériel absent** ; `MaterialGuideGenerator`) + tâche `InspectTrainingMaterialTask` (thin-wrapper : lit les `*.json` du `materialDir`, n'échoue **que** si une exigence est déclarée non satisfaite) + extension +4 properties (`materialRemoteUrl` vide par défaut, `materialCurrentVersion`, `materialDir` → `material`, `materialExpectedTypes`) + `InstallerPlatform.material` + scaffold `MATERIAL.md` **conditionnel** sous `$APP_DIR/project/` (source unique bash/batch, parité structurelle Linux/Windows).

*Dogfooding (preuve réelle)* : consommateur transitoire `/tmp` + pivots EPIC K réels → **READY** (SPG+SLIDES), **FAIL** actionnable (`outdated (expected version v2.0)`), **FAIL** (`missing QUIZ`), **SUCCESS** `nothing to verify` (backward compat) ; `MATERIAL.md` généré + `sh -n` VALIDE + `grep pullMaterial` = 0. **Découverte du dogfooding** : `materialExpectedTypes` renseigné **sans** remote était traité comme « pas d'exigence » → silencieusement ignoré ; correctif TDD `hasRequirement()`.

*Frontière vérifiable mécaniquement* : `grep ProcessBuilder` / git dans le code academy prod = **0**.

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-4-0 | Cadrage `CADRAGE_EPIC_ACADEMY_4_MATERIAL.adoc` — frontière des verbes corrigée, 9 constats, D-ACADEMY-4-1..12 ✅ S-012
| ACADEMY-4-1 | Domaine pur `academy.material` (lecture N0 + pivot EPIC K + verdict sealed + guide) ✅ S-012
| ACADEMY-4-2 | Extension +4 properties + tâche `inspectTrainingMaterial` + `MATERIAL.md` conditionnel ✅ S-012
| ACADEMY-4-3 | BDD `academy_material.feature` `@material` 8 scénarios + runner dédié + dogfooding réel ✅ S-012
| ACADEMY-4-2* | Alimenter le cours Moodle seedé depuis le matériel reçu — **DIFFÉRÉE (D-ACADEMY-4-12)** : exige le contrat SPG/SPD canonique (MVP0 training). À renuméroter en `ACADEMY-9`-adjacent lors du cadrage Qualiopi
|===

=== ACADEMY-2 : Intégration du contrat FormationRuntimePort

*Statut* : ✅ CLOS côté academy (S-013) — **frontière runtime actée**
(D-ACADEMY-11-1, arbitrage pilote 2026-09-21).

La boucle runtime (`FormationRuntimePort`, `InteractionProtocol`) n'est **pas**
implémentée par academy. Trois sources concordantes le tranchent :
`VISION_FORMATION_INTERACTIVE.adoc` § D3 + itération 9 (« Split — academy =
infra OSS, **edster/workspace-gradle = runtime agentique privé**, LocalAdapter +
CloudAdapter »), la KDoc `bureau/BureauPlugin.kt:24`, et la règle licence
3 étages (`AGENTS.adoc` : « orchestration agentique = privé, la marge »).
`VISION_PIPELINE_FORMATION` § 6 (« academy = implémenteur ») est **contredit**
par ces trois sources.

Ce qui **est** livré côté academy : le contrat `ToolExposure` (N0) est branché —
`OpenCodeCatalog.exposure()` le lit et `LearnerGuideGenerator` en dérive le
guardrail `foundry/public` (fin du triple énoncé commentaire/prose/test).

=== ACADEMY-3 : Intégration du contrat SessionMemoryContract

*Statut* : ✅ CLOS côté academy (S-013) — **l'implémenteur existe déjà**.

Le TODO « `SessionMemoryContract` : zéro implémenteur » (VISION § 6, repris
S-012) est **périmé** : `codex-plugin/src/main/kotlin/codex/profile/PgVectorSessionMemory.kt:37`
implémente le contrat N0 depuis S-094 (EPIC CDX-RC-04) — persistance pgvector,
embedding ONNX `AllMiniLmL6V2`, tâche `persistLearnerProfile`. Le VISION § 6
était vrai à l'écriture ; il ne l'est plus.

=== ACADEMY-11 : Injection du matériel pédagogique dans Moodle

*Statut* : ✅ **TERMINÉ 4/4 US (S-016)** — **arbitrage pilote 2026-09-21** : academy
**consomme** la formation produite par training et **installe le runtime Docker**
pour l'acheteur ; il **injecte de la data structurée** AsciiDoc dans Moodle — il
**n'implémente pas** la boucle runtime.

*Découverte décisive (audit image S-013)* : l'image
`erseco/alpine-moodle:v5.2.3` **embarque des outils natifs** —
`moodle-blueprint` (`/usr/local/bin/moodle-blueprint`, format Moodle Playground,
appliqué **automatiquement au démarrage** via `MOODLE_BLUEPRINT` ; étapes
implémentées `createCategory`/`createCourse`/`createUser(s)`/`enrolUser`/
`setConfig`/`installTheme`…, mais `createSection`/`addModule` = **PLANNED**) et
**`moosh`** (`/usr/local/bin/moosh`, Moodle Shell : `course-create -r`
**idempotent**, `activity-add`, `section-config-set`, `course-restore`).
L'applicateur **pilote ces outils** : **zéro PHP maison, zéro SQL, zéro DDL,
zéro stack**.

*Décisions* : `CADRAGE_EPIC_ACADEMY_11_MOODLE.adoc` (D-ACADEMY-11-1..10).
Structurantes : frontière runtime actée ; mécanisme = outils natifs de l'image ;
domaine pur `academy.moodle` ; academy **ne parse pas** le `.adoc` (payload
opaque lu Moodle-side) ; service one-shot (pattern `moodle-seed`) ; **dégradé
par défaut** ; mapping type→activité (SPG→label, SPD→page, QUIZ→quiz,
DOCUMENT→resource, SLIDES/CAPSULE→url) ; type inconnu **ignoré, jamais deviné** ;
extension +4 properties opt-in ; plan = contrat stable, applicateur générique.

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-11-0 | ✅ Cadrage `CADRAGE_EPIC_ACADEMY_11_MOODLE.adoc` (S-013)
| ACADEMY-11-1 | ✅ Domaine pur `academy.moodle` (6 fichiers, 20 unit) — S-013
| ACADEMY-11-2 | ✅ Tâche `generateMoodleImport` + extension +4 properties (7 unit) — S-013
| ACADEMY-11-3 | ✅ BDD `academy_moodle.feature` 10 scénarios + dogfooding réel Moodle — S-013
| ACADEMY-11-4 | ✅ Service compose one-shot `moodle-material` (applique `ingest.sh` après le schéma Moodle) + volume partagé `moodle-html` + garde de rejeu `sha256` + 2 scénarios BDD + dogfooding `compose up` réel — S-016 (3 bugs réels attrapés)
|===

*Note* : l'US « alimenter le seed Moodle » (ancienne ACADEMY-4-2*, différée
D-ACADEMY-4-12) **est absorbée ici** : c'est précisément l'objet d'ACADEMY-11.

=== ACADEMY-12 : Provisionnement de l'engine conteneurs par OS

*Statut* : 🟡 EN COURS 3/4 US (S-015) — 12-0 ✅ S-014 + 12-1 ✅ S-014 + 12-2 ✅ S-015 ; **12-3 à faire**.

*Source* : Session 014 (**arbitrage pilote 2026-09-25**). L'installeur suppose
Docker déjà présent ; ACADEMY-12 le **provisionne** par OS, Portainer partout.
Cadrage : `CADRAGE_EPIC_ACADEMY_12_WINDOWS.adoc`.

*Découverte décisive* : Docker Engine natif Windows ne fait pas tourner les
images Linux (`docs.docker.com/engine/install/`), « sans WSL » est impossible
(Docker Desktop **gère** WSL2, activation admin unique + reboot possible), et
« Engine sans Desktop » sur macOS est impossible (VM obligatoire → Colima).
Le **runner GitHub Windows a été écarté** (conteneurs Windows only).

*Décisions* : `CADRAGE_EPIC_ACADEMY_12_WINDOWS.adoc` (D-ACADEMY-12-1..8).
Structurantes : matrice d'engine par OS ; Portainer partout (**supersede
D-ACADEMY-6-12** — erreur de catégorie engine ≠ GUI) ; décision **pure** vs
sonde adapter ; verdict sealed jamais jeté ; contrainte reboot assumée
(installateur idempotent/resumable) ; un `install.sh` portable + queue commune ;
frontière de test explicite ; **P0 `settings.gradle.kts` tolérant** ; `HostOs` dédié.

[cols="2,3", options="header"]
|===
| US | Description
| ACADEMY-12-0 | ✅ Cadrage `CADRAGE_EPIC_ACADEMY_12_WINDOWS.adoc` + préalable P0 `settings.gradle.kts` tolérant à l'absence de creds (S-014)
| ACADEMY-12-1 | ✅ Domaine pur `academy.env` (`HostOs`/`EnvironmentFacts`/`DockerStrategy` sealed/`DockerStrategyDecider`) + BDD 7 scénarios `@env` (S-014)
| ACADEMY-12-2 | ✅ Writers `install.sh` unique (en-tête Linux apt / macOS Colima, **queue commune verbatim**) + `install.bat` (sonde admin+WSL2, WSL2/Desktop resumable) + port `EnvironmentProbe` + adapter `HostEnvironmentProbe` + `HostOs.fromOsName` + task `checkContainerEngine` (verdict rapporté) + functional TestKit + dogfooding Linux (S-015)
| ACADEMY-12-3 | 🟡 Dogfooding environnement Windows réel (`install.bat`, `docker compose config`, parité, images tirées) — jamais livré sans preuve
|===

=== ACADEMY-10 : Migration du bridge Ktor → Spring Boot WebFlux (cadré S-012)

*Statut* : 🟡 CADRÉ (S-012) — **décision pilote 2026-09-21** : le bridge
livré en ACADEMY-8 (Ktor 3.2.0 Netty) migre vers la pile
**Spring Boot WebFlux + kotlinx-coroutines + R2DBC + PostgreSQL (Docker) +
Jackson 3**.

*Source vérifiée S-012 (jamais supposée)* :

- Spring Boot **4.1.1** est la dernière GA (Maven Central) ; `4.1.1` embarque
  **Jackson 3.1.5** comme `jackson-bom` par défaut — **Jackson 3 est la
  condition de la pile** : Spring Boot 3.4.2 (déjà dans le BOM workspace)
  livre Jackson **2.18.2**, incompatible avec l'exigence « jackson3 ».
- Jackson 3 = changement de coordonnées : `tools.jackson.core:jackson-databind`
  (et non `com.fasterxml.jackson`) — vérifié Central (3.2.2 latest).
- R2DBC : `spring-boot-starter-data-r2dbc` + `org.postgresql:r2dbc-postgresql`
  (4.1.1 pin `1.1.2.RELEASE`) + `io.r2dbc:r2dbc-pool`.
- Le bridge actuel est **342 lignes de domaine pur** (`academy.bridge` : 7
  fichiers) + `WebhookBridgeServer` (114 lignes Ktor) + `ServeWebhookBridgeTask`
  (72 lignes). Le domaine est **déjà transport-agnostique** (D-ACADEMY-8-4) :
  il survit tel quel à la migration.

*Décisions à acter au cadrage (à confirmer en session de cadrage dédiée)* :

- Le **domaine pur** (`MoodleEvent`, `BridgeEventTranslator`, `BridgeRoutes`,
  `BridgeServerConfig`, `ByokBridgeSupport`) est **conservé verbatim** —
  seule la couche serveur migre (anti-corruption préservée).
- `BridgeWire` (anti-corruption `@Serializable`) migre de kotlinx-serialization
  vers **Jackson 3** : le bridge devient cohérent avec le reste de l'écosystème
  JVM (codebase/codex utilisent Jackson).
- Persistance optionnelle en PostgreSQL (Docker) : les événements routés
  peuvent être **journalisés** (traçabilité Qualiopi ACADEMY-9). Le choix
  « journaliser ou non » et le schéma (table `bridge_events`) restent à
  trancher — c'est le volet R2DBC de l'EPIC.
- Le bridge reste **opt-in, local, mono-utilisateur** (D-ACADEMY-8-2/8-7) :
  la migration change la pile, pas la doctrine.

*Options à évaluer au cadrage* :

- Remplacer `serveWebhookBridge` (tâche Gradle daemon) par un `main()`
  Spring Boot classique, ou conserver la tâche Gradle lançant le contexte
  Spring — la doctrine « artisan de build mono-utilisateur » pèse pour la
  seconde.
- WebFlux réactif (`Mono`/`Flux`) vs coroutines Kotlin (`suspend`) : WebFlux
  supporte nativement les deux ; l'écosystème cccp est **coroutine-first**
  (koog/langchain4j adapters), ce qui pèse pour `suspend`.

*Dépendances* : ACADEMY-8 (bridge Ktor) ✅ — l'EPIC est une **migration**, pas
une création. `spring-boot` 3.4.2 est déjà déclaré dans le BOM workspace
(MEMPHIS), mais **insuffisant** (Jackson 2) : un bump BOM vers Boot 4.1.1
sera probablement requis (cross-borough MEMPHIS).

*Référence de code vérifiée S-012* — `/home/cheroliv/sources/draft/api`
(draft personnel, **pattern source, pas source de version** — il tourne en
Spring Boot 3.5.9/Jackson 2.18.3, donc **antérieur à l'exigence jackson3**) :

- **Application** : `@SpringBootApplication` + `@EnableConfigurationProperties`
  + `runApplication<Server>(*args)`, classe `Server` minimale (`app/Server.kt`,
  18 lignes).
- **Entrée CLI** : `CommandLineRunner` + profil dédié (`app/CommandLine.kt`,
  `runBlocking`). C'est le pattern qui répond à la question « tâche Gradle
  daemon vs `main()` » : le draft a **les deux** (`Server` web + `CommandLine`
  CLI, profils distincts) — à reprendre pour conserver `serveWebhookBridge`.
- **R2DBC** : `DatabaseConfiguration` (`@EnableR2dbcRepositories`,
  `R2dbcTransactionManager`, `TransactionalOperator`, custom conversions
  `Instant ↔ LocalDateTime`) + `ConnectionFactoryInitializer` **par profil**
  (`local`/`dev`/`test`/`prod`/`cloud-dev`) chargeant le DDL.
- **DDL co-localisé au domaine** : `UserActivation.Relations` porte
  `SQL_SCRIPT` / `INSERT` / `UPDATE` (`CREATE TABLE IF NOT EXISTS`, index,
  FK `ON DELETE CASCADE`) — même philosophie que `StoreStatements` de codebase.
  Directement transposable en `BridgeEvent.Relations` (table
  `bridge_events`).
- **DAO** : `DatabaseClient` injecté, `sql(...).bind(...).fetch()
  .awaitRowsUpdated()` en `suspend`, résultat `Either<Throwable, Long>`
  (Arrow). Le draft utilise Arrow (`arrow-core`, `arrow-fx-coroutines`) —
  **choix à arbitrer** : l'écosystème cccp est aujourd'hui sans Arrow
  (exceptions + sealed results), un `Result`/sealed maison serait plus
  cohérent.
- **Controller** : `@RestController` + `@RequestMapping` + `suspend fun`
  (Spring WebFlux supporte nativement les coroutines) +
  `MediaType.APPLICATION_PROBLEM_JSON_VALUE` / `ProblemDetail` (RFC 7807,
  `spring.webflux.problemdetails.enabled: true`). Remplace avantageusement le
  `400`/`404` manuels de Ktor.
- **Config** : `spring.r2dbc.url = r2dbc:postgresql://host:5432/db?currentSchema=…`
  + `server.port` + `spring.profiles.default`.
- **PostgreSQL Docker** : `compose.yml` minimal (`postgres:16.3`, env
  `POSTGRES_DB/PASSWORD/USER`, port 5432) — le scaffold academy a déjà
  `postgres:16` (ACADEMY-6), à **réutiliser**, non dupliquer.
- **Non repris du draft** (hors périmètre) : Spring Security + JWT, Thymeleaf,
  mail, JJWT, `spring-cloud-contract`, langchain4j-spring, Arrow. Academy
  reste **local, mono-utilisateur, sans auth** (D-ACADEMY-8-2).

*Note* : cet EPIC n'est **pas** dans le chemin critique MVP0. Il se justifie
par la cohérence de pile (un seul sérialiseur Jackson dans l'écosystème, un
seul modèle de concurrence coroutines) et par la traçabilité Qualiopi
(ACADEMY-9) qu'une persistance PostgreSQL rend possible. À ordonnancer sur
décision pilote.

'''
== Décisions structurantes

|===
| ID | Décision | Session
| D-ACADEMY-1 | La ressource academy = machine d'expérimentation complète du pipeline cccp.education : hôte = Docker + Java 25 + Gradle 9.7.1 ; Docker = Moodle + PostgreSQL + Ollama + Portainer + workspace opencode zsh. Sans VPS, mono-utilisateur. | S-002
| D-ACADEMY-2 | L'installateur par plateforme (sh/bat) provisionne l'hôte, génère compose, seede la DB Moodle et les volumes, expose opencode.json — le premier `gradle build` dogfood l'écosystème. | S-002
| D-ACADEMY-3 | Le plugin academy est un générateur de ressource de distribution — il ne réplique pas KINSHASA (provisionnement dev), qui reste N0. | S-002
| D-ACADEMY-4 | `com.example.installer` (proposition externe) rejetée : l'identité reste `education.cccp.academy`, code anglais only, TDD/BDD baby-step strict. fichiers `files/` et `files.zip` supprimés. | S-002
| D-ACADEMY-6-14 | Image Moodle **réelle** `erseco/alpine-moodle:v5.2.3` (auto-install, PostgreSQL natif) ; suivre le **latest** (5.2.3). `moodle:4.5` était un placeholder inexistant (dogfooding S-007). | S-007
| D-ACADEMY-6-15 | Contrat d'environnement Moodle **réel** : `DB_TYPE/DB_HOST/DB_NAME/DB_USER/DB_PASS` + `MOODLE_USERNAME/PASSWORD/SITENAME` (jamais `MOODLE_DATABASE_*`, faux). Valeurs `${...}` depuis `.env`. | S-007
| D-ACADEMY-6-16 | `portainer/portainer-ce` (maintenue) ; service `workspace` **retiré du compose actif** tant que `cccp-education/academy-workspace` n'est pas publiée (TODO ACADEMY-5). | S-007
| D-ACADEMY-6-17 | **Pas de healthcheck Portainer** : image *distroless* (ni `sh` ni `wget`) → sonde `wget` invalide (`unhealthy` alors que HTTP 200). | S-007
| D-ACADEMY-6-18 | **Application de D-6-10 — contenu OS aligné** : le scaffold ne provisionne plus Java Temurin ni Gradle sur l'hôte (retirés des 3 scripts) ; `JAVA_VERSION`/`GRADLE_VERSION` = pins documentaires de l'image workspace ; Docker = seule exigence hôte (check + exit non-zéro) ; macOS = Homebrew conditionné. ACADEMY-6-1/2/3 CADUQUES. | S-008
| D-ACADEMY-5-1 | **Workspace = `Dockerfile` généré sur `gradle:9.7.1-jdk25`** (Ubuntu 26.04 : bash+git+curl+unzip+apt-get+Java 25+Gradle 9.7.1), installant `zsh` + `opencode` (script officiel), utilisateur `learner`, `WORKDIR /workspace`. L'image officielle `ghcr.io/anomalyco/opencode` est Alpine-minimal (ni shell évolué, ni git, ni Java/Gradle) → écartée. Construite par `docker compose build`, zéro registry. | S-009
| D-ACADEMY-5-2 | **`opencode.json` généré** (rendu pur) : `$schema`, `model` (`ollama/<modèle>`), `provider` (Ollama embarqué `http://ollama:11434/v1`), `instructions` (`AGENTS.md`). Guardrail `foundry/public` porté par le catalogue + documenté, pas par une clé `permission` (ineffective). Zéro secret (BYOK = ACADEMY-7). | S-009
| D-ACADEMY-5-3 | **Domaine pur `education.cccp.academy.opencode`** (`OpenCodeConfig`/`OpenCodePlugin`/`OpenCodeCatalog`/`OpenCodeConfigGenerator`/`LearnerGuideGenerator`/`WorkspaceDockerfileGenerator`) — JSON par concaténation déterministe, zéro lib de sérialisation. | S-009
| D-ACADEMY-5-4 | **`AGENTS.md` apprenant généré** (ancre officielle opencode) : identité, inventaire des tâches exposées (lu du catalogue), workflow (compose up → opencode → tâche). | S-009
| D-ACADEMY-5-5 | **Extension +3 properties** : `openCodeEnabled` (true), `openCodeModel` (`gpt-oss:120b-cloud`), `openCodeProviderUrl` (`http://ollama:11434/v1`). Backward compat totale. | S-009
| D-ACADEMY-5-6 | **Service `workspace` rétabli** (`build: ./project`, image `cccp-education/academy-workspace:latest`, `./project:/workspace`, `depends_on: ollama`, `profiles: [agent]` — opt-in `docker compose --profile agent up`). | S-009
| D-ACADEMY-5-7 | **`opencode.json`+`AGENTS.md`+`Dockerfile` sous `$APP_DIR/project/`** (source unique bash/batch → parité structurelle Linux/Windows, leçon S-005/S-006). | S-009
| D-ACADEMY-5-8 | **Implémentation des ports N0 hors session** : `FormationRuntimePort`/`InteractionProtocol` exigent BYOK (ACADEMY-7) + bridge Ktor (ACADEMY-8) → séquencés en ACADEMY-2/3. ACADEMY-5 livre l'exposition. | S-009
| D-ACADEMY-5-9 | **Aucune dépendance compile ajoutée** (D-ACADEMY-6-8 reconduite) : ni Jackson, ni runtime-contracts. Zéro bump BOM. | S-009
| D-ACADEMY-5-10 | **Exposition = 11 plugins publics catalogués** (`planner,codex,codebase,slider,document,capsule,plantuml,graphify,hyperframes,bakery,readme`), tâches vérifiées source. Zéro `foundry/private` (training, edster, workspace, waiter, quizz). | S-009
| D-ACADEMY-5-11 | **opencode installé EN TANT QUE `learner`** : un install en root atterrit dans `/root/.opencode` (invisible au learner) → `USER learner` avant l'install + `ENV HOME=/home/learner`. Découvert par dogfooding réel. | S-009
| D-ACADEMY-7-1 | **`runtime-contracts` (N0) devient une dépendance compile** — supersede D-ACADEMY-5-9 : ACADEMY-7 est le moment désigné pour consommer `ByokLlmConfig`/`LlmProviderKind`. Coût transitif mesuré (`opencode-session-contracts:0.0.2` + `i18n-contracts:0.0.2`, Central 200), zéro bump BOM. | S-010
| D-ACADEMY-7-2 | **Zéro secret — la clé est un NOM, jamais une valeur** : `opencode.json` rend `options.apiKey = "{env:<VAR>}"` (syntaxe officielle opencode). La valeur n'apparaît ni dans le JSON, ni dans `.env`, ni dans le compose. Prouvé par dogfooding (dummy key absente du JSON). | S-010
| D-ACADEMY-7-3 | **Le contrat N0 valide, l'academy mappe** : les invariants de `ByokLlmConfig` sont le guardrail de fail-fast, l'academy ne les duplique pas. | S-010
| D-ACADEMY-7-4 | **Domaine pur `education.cccp.academy.byok`** : `ByokProviderSpec` + `ByokProviderCatalog` (`LlmProviderKind` → spec), endpoints/npm officiels vérifiés S-010. Le domaine `opencode` consomme le catalogue — plus de `const PROVIDER_KEY`. | S-010
| D-ACADEMY-7-5 | **`OpenCodeConfig` étendu de façon additive** (+`provider` défaut `OLLAMA_LOCAL`, +`apiKeyEnvVar` défaut null) : la sortie par défaut est **byte-identique** ACADEMY-5, verrouillée par un test de caractérisation. | S-010
| D-ACADEMY-7-6 | **Extension +2 properties** : `openCodeProvider` (défaut `OLLAMA_LOCAL`), `openCodeApiKeyEnvVar` (défaut vide = défaut par provider). `openCodeProviderUrl` sert de `baseUrl`. Backward compat totale. | S-010
| D-ACADEMY-7-7 | **`CUSTOM` exige un `baseUrl`** (plus strict que N0) : un provider OpenAI-compatible sans endpoint n'est pas adressable → fail-fast. | S-010
| D-ACADEMY-7-8 | **`.env` documente le nom de la variable** (valeur vide) et **le compose la transmet** au service `workspace`, uniquement quand le provider exige une clé. Parité structurelle Linux/Windows conservée. | S-010
| D-ACADEMY-7-9 | **`poolPorts` (N0) n'est pas rendu** : c'est une concern de rotation runtime (codebase N1 `LlmProviderResolver`), l'agent parle au endpoint cloud officiel. | S-010
| D-ACADEMY-7-10 | **`AGENTS.md` rend la config concrète** : fournisseur, modèle, nom exact de la variable à exporter (« aucun — runtime local embarqué » pour `OLLAMA_LOCAL`). | S-010
| D-ACADEMY-8-1 | **Le bridge Ktor est un transport ADDITIONNEL, pas un remplacement** : le daemon stdin/stdout de codebase reste la voie existante ; le bridge sert au producteur **externe**. Corrige la justification inexacte de la vision § 8 D4. | S-011
| D-ACADEMY-8-2 | **Ktor `implementation` assumé et local** : serveur lancé par la tâche `serveWebhookBridge` (daemon, pattern `SessionProtocolDaemonTask`), jamais hébergé — « aucun VPS / mono-utilisateur ». | S-011
| D-ACADEMY-8-3 | **Sérialisation kotlinx** (`ktor-serialization-kotlinx-json` + plugin kotlin-serialization) : recevoir du JSON HTTP exige une vraie sérialisation (le rendu par concaténation de D-ACADEMY-5-3 ne s'applique pas à l'entrée). | S-011
| D-ACADEMY-8-4 | **Domaine pur `education.cccp.academy.bridge`** (`MoodleEvent`, `BridgeEventTranslator`, `BridgeRoutes`), testable sans HTTP ni serveur. | S-011
| D-ACADEMY-8-5 | **Contrat d'entrée propre à academy, pas un plugin tiers** : aucune dépendance à `local_webhooks` ni à un payload Moodle non vérifié (leçon S-007). | S-011
| D-ACADEMY-8-6 | **Routes minimales** (`GET /health`, `POST /events/moodle`, `POST /session`) ; le bridge **route et acquitte, il ne pense pas** (zéro LLM, boundary respectée). | S-011
| D-ACADEMY-8-7 | **Extension +3 properties** : `bridgeEnabled` (défaut `false` — opt-in strict), `bridgeHost` (`127.0.0.1`), `bridgePort` (`8765`). Backward compat totale. | S-011
| D-ACADEMY-8-8 | **Tâche `serveWebhookBridge`** (groupe `academy`), garde `onlyIf { bridgeEnabled }` — un build n'ouvre jamais un port par accident. | S-011
| D-ACADEMY-8-9 | **Scaffold conditionnel** : `AGENTS.md` documente le bridge (routes, host/port, `curl`) **uniquement** si `bridgeEnabled`. | S-011
| D-ACADEMY-8-10 | **Tests sans port réel** (`testApplication`), **port réel uniquement pour le dogfooding** (preuve d'exécution, leçon S-007). | S-011
| D-ACADEMY-8-11 | **Anti-corruption `BridgeWire`** : les contrats N0 (`contracts.session.*`) ne sont pas `@Serializable` et le restent — le bridge mappe sa propre vue `@Serializable` au bord, sans réannoter le contrat partagé (découvert par un RED de comportement en GREEN). | S-011
| D-ACADEMY-4-1 | **Frontière des verbes tranchée (corrige le BACKLOG)** : le **bureau** implémente `MaterialUpdateResolver` (transport git, `BUREAU-3`) ; **academy lit** `MaterialUpdateContract` comme déclaration d'exigence. Academy ne réimplémente **jamais** git. | S-012
| D-ACADEMY-4-2 | **`MaterialUpdateContract` (N0) = l'exigence consommée** : academy en dérive `MaterialRequirement`. Zéro nouvelle dépendance (N0 sur le classpath depuis ACADEMY-7). | S-012
| D-ACADEMY-4-3 | **Domaine pur `education.cccp.academy.material`** — zéro type Gradle, zéro git, zéro I/O réseau : une fonction pure de l'exigence + des manifestes vers un verdict. | S-012
| D-ACADEMY-4-4 | **Le pivot lu est `metadata.json` (EPIC K)** — contrat normé réutilisé, jamais un second format inventé. Lecture tolérante (`ignoreUnknownKeys`) : un champ ajouté par un producteur ne casse pas le consommateur. | S-012
| D-ACADEMY-4-5 | **Sérialisation kotlinx pour l'entrée** (déjà présente depuis ACADEMY-8) : lire un manifeste exige un vrai décodeur. Zéro nouvelle dépendance. | S-012
| D-ACADEMY-4-6 | **`MaterialArtifactType` = le vocabulaire des livrables actés** (SPG/SPD/SLIDES/DOCUMENT/CAPSULE/QUIZ), mappé 1:1 du champ `type` du pivot, casse normalisée. Une valeur inconnue n'est **jamais devinée** (`null`, ignorée du verdict). | S-012
| D-ACADEMY-4-7 | **`MaterialReadiness` sealed, verdict pur** (`Ready`/`Incomplete`/`Empty`) : l'inspecteur **ne lève jamais** sur du matériel absent — la ressource est générée avant le pull. | S-012
| D-ACADEMY-4-8 | **Extension +4 properties** (`materialRemoteUrl` défaut vide, `materialCurrentVersion`, `materialDir` → `material`, `materialExpectedTypes`). Backward compat totale. | S-012
| D-ACADEMY-4-9 | **Tâche `inspectTrainingMaterial`** : échoue **uniquement** si une exigence est déclarée (`materialRemoteUrl` **ou** `materialExpectedTypes`) non satisfaite. Sans exigence : rapport `Empty`, succès. | S-012
| D-ACADEMY-4-10 | **Le scaffold déclare l'exigence, il ne transporte pas** : `MATERIAL.md` documente le remote, la version et le **mécanisme figé** (git pull de la version taguée), **jamais** une commande `pullMaterial` inexistante (BUREAU-3 🔴 À FAIRE, leçons S-007/S-011). | S-012
| D-ACADEMY-4-11 | **Surface de test academy = parsing + verdict** : le transport git appartient au bureau et est testé par lui. | S-012
| D-ACADEMY-4-12 | **Alimenter le seed Moodle est DIFFÉRÉ** : le seed crée la coquille du cours ; énumérer modules/séances exige le contrat SPG/SPD canonique (MVP0 training non fait). L'implémenter maintenant produirait un scaffold non exécutable (leçon S-007). | S-012
|===

'''
== Décisions ACADEMY-12 — Provisionnement de l'engine par OS (S-014)

|===
| ID | Décision | Session
| D-ACADEMY-12-1 | **Matrice d'engine par OS** : Linux = Docker Engine natif ; Windows = Docker Desktop (`install --user --quiet --accept-license --backend=wsl-2`, WSL2 requis et géré) ; macOS = **Colima** (MIT). **Portainer partout** (uniformité apprenant, même URL `:9000`). **Supersede D-ACADEMY-6-12** (« Portainer remplace Docker Desktop » = erreur de catégorie : engine ≠ GUI). | S-014
| D-ACADEMY-12-2 | **Décision pure, exécution adapter** : domaine `academy.env` sans I/O ; la sonde (OS/admin/WSL2/daemon) appartient à l'adapter de la tâche. Testable sans machine. | S-014
| D-ACADEMY-12-3 | **`DockerStrategy` sealed** : `AlreadyReady`/`InstallDockerEngine`/`InstallColima`/`InstallDockerDesktop`/`BootstrapWsl2First`/`Unsupported`. Le verdict ne lève jamais (pattern `MaterialReadiness`). | S-014
| D-ACADEMY-12-4 | **Contrainte reboot assumée** : l'activation WSL2 exige admin + reboot possible → installateur **idempotent/resumable**. Ne jamais promettre « un run sans reboot ». | S-014
| D-ACADEMY-12-5 | **Un `install.sh` portable** (en-tête par OS, queue commune `compose up` + `.env` + seed via `composeBody()`) + un `install.bat`. Zéro duplication du compose. | S-014
| D-ACADEMY-12-6 | **Frontière de test explicite** : décision testable sans machine (unit + BDD), syntaxe (`sh -n`), dogfooding OS réel. macOS vérifié structurellement (pas de Mac à disposition). | S-014
| D-ACADEMY-12-7 | **P0 `settings.gradle.kts` tolérant** : nmcp appliqué seulement si les 2 credentials présents (un hôte nu configure). Les 25 boroughs partageaient le blocage. | S-014
| D-ACADEMY-12-8 | **`TargetOs` non réutilisé** : `HostOs` dédié au contexte `env` (deux contextes bornés, deux enums). | S-014
|===

'''
== Historique des mises à jour

|===
| Date | Description
| 2026-09-19 | Création initiale du BACKLOG avec les Épics placeholder basés sur VISION_PIPELINE_FORMATION.adoc
| 2026-09-19 | S-001 : ajout EPIC ACADEMY-0-BIS ✅ TERMINÉ (architecture 0124, gouvernance, licence, repo)
| 2026-09-20 | S-002 : recadrage de l'orientation — vision hybride actée (D-ACADEMY-1..4), suppression files/ + files.zip, ACADEMY-1 pivoté vers socle générateur, ACADEMY-6 révisé en « installeur hybride » (US-0 cadrage ✅), ordre des Épics réordonné
| 2026-09-20 | S-003 : ACADEMY-1 TERMINÉ 3/3 US (extension Property-based, domaine pur, 4 tâches, 17 checks verts — check VERT + validatePlugins + publishToMavenLocal) ; ACADEMY-6-0 cadrage rédigé (CADRAGE_EPIC_ACADEMY_INSTALLER.adoc) ; push origin cccp-education/academy-gradle
| 2026-09-22 | S-004 : révisions pilote actées sur le scaffold compose — (1) Ollama embarqué (D-ACADEMY-6-11), (2) Portainer remplace Docker Desktop (D-ACADEMY-6-12, transverse Windows), (3) **PostgreSQL remplace MariaDB** (D-ACADEMY-6-13). ACADEMY-6-4 aligné (parité Linux/mac + Windows). |
| 2026-09-22 | S-005 : **ACADEMY-6-4 TERMINÉ** — compose complet (network `academy-net`, volumes nommés `postgres-data`/`ollama-models`/`portainer-data`, healthcheck PostgreSQL `pg_isready` + `service_healthy`, Moodle `pgsql`, `.env` noms-seuls) ; REFACTOR `composeBody()`/`envBody()` = source unique partagée bash/batch (**parité structurelle**) ; tests 43 checks 0 FAIL ; dogfooding `docker compose config` VALIDE. |
| 2026-09-22 | S-006 : **ACADEMY-6-5 TERMINÉ** — seed DB data-only (service one-shot `moodle-seed`, entrypoint idempotent `seed.sh`, `seed-course.sql` `INSERT ... WHERE NOT EXISTS`) ; REFACTOR `seedScriptBody()`/`seedCourseSqlBody()` = source unique partagée + `escapeCmd()` (parité structurelle) ; 16 unit + 4 functional + 11 scénarios cucumber 0 FAIL ; dogfooding `docker compose config` + `sh -n` VALIDES + idempotence SQL prouvée PostgreSQL 16 réel (run1 `INSERT 0 1`, run2 `INSERT 0 0`). |
| 2026-09-21 | S-007 : **ACADEMY-6-6 TERMINÉ** — dogfooding bout-en-bout. Le run réel a prouvé l'enchaînement (PostgreSQL/Ollama/Moodle healthy, seed `INSERT 0 1`→`INSERT 0 0`, Moodle **5.2.3** `/login` HTTP 200) et révélé **5 écarts** du scaffold inexécutable : `moodle:4.5` inexistante, contrat env `MOODLE_DATABASE_*` faux, version périmée, `portainer/portainer` obsolète, healthcheck Portainer invalide (distroless) + workspace à image non publiée. Corrigés (D-ACADEMY-6-14..17) — image `erseco/alpine-moodle:v5.2.3`, contrat `DB_*`/`MOODLE_*`, `portainer-ce` sans healthcheck, workspace différé (TODO ACADEMY-5). RED 20 failed → GREEN. Gates : 21 unit + 5 functional + 15 scénarios cucumber 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT. |
| 2026-09-21 | S-008 : **ACADEMY-6-7 TERMINÉ + EPIC ACADEMY-6 TERMINÉ** — Cucumber BDD étendu (contenu OS, chemins, idempotence, erreurs, `opencode.json` différé) + alignement du contenu OS sur D-ACADEMY-6-10 (hôte = Docker uniquement, zéro provisioning Java/Gradle hôte) ; ACADEMY-6-1/2/3 **CADUQUES** absorbés (D-ACADEMY-6-18). RED prouvé aux 3 niveaux (unit 6 failed / cucumber 6 failed / functional 1 failed) → GREEN. Gates : 28 unit + 6 functional + 21 scénarios cucumber 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, dogfooding `sh -n` + `docker compose config` VALIDES. |
| 2026-09-21 | S-009 : **EPIC ACADEMY-5 TERMINÉ** — exposition agent opencode. Découvertes : image officielle opencode Alpine-minimal (inapte Gradle), base réelle `gradle:9.7.1-jdk25`. Domaine pur `academy.opencode` (6 fichiers, 11 plugins publics catalogués source-vérifiés) + service `workspace` (profile agent, TODO S-007 résolu) + `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` générés (parité Linux/Windows) + extension 3 properties ; ACADEMY-5-2 absorbée. Dogfooding réel : `docker build` SUCCÈS, `opencode 1.18.31` learner, `sh -n`+`docker compose config`+`jq` VALIDES ; bug install-root corrigé (D-ACADEMY-5-11). Gates : 50 unit + 7 functional + 21 scénarios installer + 5 scénarios opencode 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT. |
| 2026-09-21 | S-010 : **EPIC ACADEMY-7 TERMINÉ** — BYOK LLM multi-provider. Faits vérifiés S-010 : contrat N0 `runtime-contracts:0.0.1` sur Central (JAR inspecté), interpolation `{env:NAME}` officielle opencode, ids providers réels models.dev (`ollama`/`ollama-cloud`/`google`/`huggingface`). Domaine pur `academy.byok` (`ByokProviderSpec` + `ByokProviderCatalog` mapping total) + `OpenCodeConfig` +2 champs (défaut byte-identique ACADEMY-5, test de caractérisation) + `OpenCodeConfigGenerator` multi-provider (`apiKey` toujours `{env:NAME}`) + `LearnerGuideGenerator` BYOK concret + extension +2 properties + `.env` nom-seul + compose transmet la variable au service `workspace` ; dépendance N0 (supersede D-ACADEMY-5-9) ; D-ACADEMY-7-1..10. Dogfooding réel GEMINI + défaut : preuve de non-fuite de la clé, `sh -n`/`compose config`/`jq` VALIDES, byte-identité ACADEMY-5. Gates : 218 unit + 9 functional + 21 installer + 5 opencode + 8 byok scénarios 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, zéro secret. |
| 2026-09-21 | S-011 : **EPIC ACADEMY-8 TERMINÉ** — webhooks Moodle + bridge Ktor (décision pilote : Ktor acté). Constats vérifiés : le bridge codebase (`SessionProtocolDaemonTask`) est **stdin/stdout, pas HTTP** (justification D4 inexacte → transport additionnel, D-ACADEMY-8-1), **Moodle n'a pas de webhook natif** → schéma academy (D-ACADEMY-8-5), Ktor 3.2.0 sur Central, `opencode-session-contracts` non transitif, contrats N0 **non `@Serializable`** (RED de comportement → anti-corruption `BridgeWire`, D-ACADEMY-8-11). Domaine pur `academy.bridge` (`MoodleEvent`/`BridgeEventTranslator` session stable/`BridgeRoutes`/`BridgeServerConfig`/`BridgeWire`/`ByokBridgeSupport`) + serveur `WebhookBridgeServer` Ktor Netty + tâche `serveWebhookBridge` **opt-in** (SKIPPED par défaut, TestKit) + extension +3 properties + `AGENTS.md` conditionnel ; D-ACADEMY-8-1..11. Dogfooding réel serveur + curl (`/health` 200, ack, 400/404, arrêt propre). Gates : 311 unit + 10 functional + 21 installer + 5 opencode + 8 byok + 6 bridge scénarios 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, zéro secret. |
| 2026-09-21 | S-012 : **EPIC ACADEMY-4 TERMINÉ** + **publication Maven Central 0.0.1**. Correction du BACKLOG (faits vérifiés) : `MaterialUpdateContract` est une **data class** (pas une interface), le **transport git appartient au bureau** (vision § 3.2), et `pullMaterial` **n'existe pas** (`bureau-gradle` = 3 fichiers, zéro `tasks.register` — mention KDoc seulement). Domaine pur `academy.material` (`MaterialArtifactType` 6 livrables, `MaterialRequirement`, `MaterialManifest` = pivot EPIC K tolérant, `MaterialReadiness` sealed, `MaterialInspector`, `MaterialGuideGenerator`) + tâche `inspectTrainingMaterial` (n'échoue que si exigence déclarée) + extension +4 properties + scaffold `MATERIAL.md` conditionnel (parité Linux/Windows) ; D-ACADEMY-4-1..12. Dogfooding réel pivots EPIC K (READY/outdated/missing/Empty) + `sh -n` + `grep pullMaterial`=0 ; **découverte** : exigence par types seuls silencieusement ignorée → correctif TDD `hasRequirement()`. **Publication Central** : nmcp 1.5.0, bundle `5c54af3f-1bfc-405f-950a-1172fff86b7a` AUTOMATIC, propagation HTTP 200 (academy-plugin + marker). **EPIC ACADEMY-10 cadré** (migration Ktor → Spring Boot WebFlux + coroutines + R2DBC + PostgreSQL + Jackson 3 ; Boot 4.1.1 = Jackson 3.1.5, Boot 3.4.2 = Jackson 2.18.2 → bump BOM probable). Gates : 152 unit + 13 functional + 21 installer + 5 opencode + 8 byok + 6 bridge + 8 material scénarios 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, nmcpCheckAggregationFiles VERT, zéro secret. |
| 2026-09-21 | S-013 : **EPIC ACADEMY-11 3/4 US** + **ToolExposure (N0) branché** + **frontière runtime actée**. Arbitrage pilote : academy **consomme** la formation (training) et **injecte de la data structurée** AsciiDoc dans Moodle — **pas** de boucle runtime ; puis « on s'appuie sur l'**image préconstruite** et ses **outils natifs** ». **Découverte décisive** : l'image `erseco/alpine-moodle:v5.2.3` embarque `moodle-blueprint` (format Moodle Playground, appliqué auto au boot via `MOODLE_BLUEPRINT`) et **`moosh`** (Moodle Shell : `course-create -r` idempotent, `activity-add`, `section-config-set`) → l'applicateur les **pilote** (zéro PHP maison, zéro SQL/DDL, zéro stack). Domaine pur `academy.moodle` (`MoodleActivityType`/`MoodleActivityMapping` vocabulaire Moodle réel, `MoodleMaterial` path-only, `MoodleActivity`/`MoodleSection`, `MoodleImportPlan`, `MoodlePlanBuilder`, `MoodleIngestionScriptGenerator`) + tâche `generateMoodleImport` (opt-in `moodleImportEnabled` défaut `false`, **dégradée** : matériel vide → rien écrit) + extension +4 properties + BDD `academy_moodle.feature` 10 scénarios + runner `cucumberTestMoodle` (S-082) ; D-ACADEMY-11-1..10. **ToolExposure** : `OpenCodeCatalog.exposure()` lit le contrat N0, le guide dérive le guardrail. **Frontière runtime actée** (D-ACADEMY-11-1) : `FormationRuntimePort` appartient à l'étage privé → ACADEMY-2/3 **CLOS côté academy** ; le TODO `SessionMemoryContract` « zéro implémenteur » est **périmé** (`PgVectorSessionMemory` codex S-094). **Dogfooding réel** Moodle 5.2.3 + PostgreSQL 16 (cours/sections/activités injectés via `moosh`) — **3 bugs réels attrapés** (double-quote shortname, champ `awk` du CSV `course-list`, `--intro` vs `--content`) + 3 tests de régression. Gates : 183 unit + 13 functional + 21 installer + 6 opencode + 8 byok + 6 bridge + 8 material + 10 moodle scénarios 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, zéro secret. Reste ACADEMY-11-4 (service compose `moodle-material`, non livré faute de preuve `compose up`). |
| 2026-09-25 | S-014 : **EPIC ACADEMY-12 cadré + ACADEMY-12-1 TERMINÉ** — provisionnement de l'engine conteneurs par OS. Demande pilote (tester l'installation Windows) **recadrée par les faits** : Docker Engine natif Windows ne fait pas tourner les images Linux (`docs.docker.com/engine/install/`), « sans WSL » est impossible (Docker Desktop *gère* WSL2, activation admin + reboot possible), « Engine sans Desktop » sur macOS est impossible (VM → Colima) ; **runner GitHub Windows écarté** (conteneurs Windows only). **Matrice d'engine actée** : Linux natif / Windows Desktop / macOS Colima, **Portainer partout** — **supersede D-ACADEMY-6-12** (erreur de catégorie engine ≠ GUI ; D-ACADEMY-12-1..8). **P0** : `settings.gradle.kts` nmcp conditionnel (un hôte nu configure ; les 25 boroughs partageaient le blocage). Domaine pur `academy.env` (`HostOs`/`EnvironmentFacts`/`DockerStrategy` sealed/`DockerStrategyDecider`) + BDD `academy_env.feature` 7 scénarios `@env` + runner `cucumberTestEnv` (S-082). RED prouvé (30 `Unresolved reference` + 7 `UndefinedStepException`). Gates : 193 unit + 13 functional + 68 scénarios Cucumber 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, `help -Duser.home=<vide>` VERT, zéro secret. Reste ACADEMY-12-2 (writers + sonde) et ACADEMY-12-3 (dogfooding Windows réel). |
| 2026-09-25 | S-015 : **ACADEMY-12-2 TERMINÉ** — writers portables + sonde adapter + dogfooding Linux. Constats (faits vérifiés) : les 2 scripts Unix étaient des quasi-doublons (D-ACADEMY-12-5 non appliquée) et **vérifiaient** Docker au lieu de le provisionner ; la procédure officielle est le **dépôt apt** (`docs.docker.com/engine/install/{ubuntu,debian}/`, pas `get.docker.com`) ; Docker Desktop Windows est scriptable **per-user** (`install --user --quiet --accept-license --backend=wsl-2`, sans admin), `wsl --status` sonde WSL2 ; Colima (`brew install colima docker` + `colima start`) pour macOS. **Gouvernance respectée** (matrice D-12-1, portabilité D-12-5, pure/adapter D-12-2, reboot D-12-4) + **ajustement D-12-3** : `checkContainerEngine` **rapporte** le verdict sans jeter (un hôte nu configure/teste — P0). **GREEN** : `unixScript` unique (en-tête Linux apt / macOS Colima, queue commune verbatim) ; `windowsScript` refondu (sonde en faits `IS_ADMIN`, `:bootstrap_wsl2` resumable / `:install_docker_desktop`) ; port `EnvironmentProbe` + adapter `HostEnvironmentProbe` (seul `ProcessBuilder` prod) + `HostOs.fromOsName` pur + task `checkContainerEngine`. **Tests** : +13 unit +1 functional (`bash -n` réel) +3 scénarios BDD (24 installer) ; contrat macOS migré Docker Desktop→Colima (supersede assumé). **Dogfooding Linux réel** : consommateur /tmp, sonde `docker info`, exécution HOME isolé → compose/`.env`/seed/project écrits, `docker compose config` **exit 0** (5 services), idempotence marquée. Gates : 207 unit + 16 functional + 69 Cucumber 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, P0 hôte nu VERT, zéro secret. Reste ACADEMY-12-3 (dogfooding Windows réel). |
|===

'''
== TODOs

[x] Push vers origin cccp-education/academy-gradle (repo vide créé, origin configuré)
[x] ACADEMY-1-0 : extension typée `academyInstaller` (pattern Property-based)
[x] ACADEMY-6-0 : rédiger `CADRAGE_EPIC_ACADEMY_INSTALLER.adoc` (frontières, contenu des scripts, seed DB, articulation 1/5/6/7)
[x] ACADEMY-6-4 : compose complet (network, volumes nommés, healthcheck PostgreSQL, `.env` noms-seuls) + parité structurelle Linux/Windows + dogfooding `docker compose config`
[x] ACADEMY-6-5 : seed DB data-only au premier `compose up` (service one-shot `moodle-seed`, entrypoint idempotent, `seed-course.sql`) + parité structurelle Linux/Windows + idempotence prouvée PostgreSQL 16 réel
[x] ACADEMY-6-6 : dogfooding conteneur éphémère (installeur Linux réel, étapes 1→7) — **TERMINÉ S-007** (5 écarts découverts + corrigés, run réel complet prouvé)
[x] ACADEMY-6-7 : Cucumber BDD étendu (contenu OS, erreurs, idempotence) — **TERMINÉ S-008** (+ alignement D-ACADEMY-6-10)
[x] ACADEMY-5 : service `workspace` réinséré dans `composeBody()` (image générée `cccp-education/academy-workspace` sur `gradle:9.7.1-jdk25`, profile `agent`) + `opencode.json` + `AGENTS.md` générés — **TERMINÉ S-009** (le TODO laissé S-007 est résolu ; la publication d'image est remplacée par un build local `docker compose build`)
[x] Recadrer ACADEMY-6-1/2/3 — **CADUQUES S-008** (absorbés par 6-7, D-ACADEMY-6-18)
[x] ACADEMY-7 : BYOK LLM — `ByokLlmConfig` (runtime-contracts N0) → `opencode.json` multi-provider (OLLAMA_LOCAL/OLLAMA_CLOUD/GEMINI/HUGGINGFACE/CUSTOM), clé **par env var, jamais la valeur** — **TERMINÉ S-010** (domaine `academy.byok`, `{env:NAME}`, `.env`→compose→JSON, dogfooding non-fuite)
[x] ACADEMY-8 : webhooks Moodle + bridge Ktor — **TERMINÉ S-011** (domaine `academy.bridge`, serveur Ktor opt-in, schéma academy, anti-corruption `BridgeWire`, dogfooding serveur+curl) ; le daemon N1 stdin/stdout n'est pas réutilisé (transport additionnel, D-ACADEMY-8-1)
[x] ACADEMY-4 : consumption du matériel versionné (exigence N0 lue + verdict + guide) — **TERMINÉ S-012** (frontière des verbes corrigée : le transport git reste au bureau BUREAU-3 ; academy lit le pivot EPIC K)
[x] Publication Maven Central d'academy — **TERMINÉE S-012** (academy-plugin + marker `education.cccp.academy` 0.0.1, bundle nmcp `5c54af3f-1bfc-405f-950a-1172fff86b7a`, AUTOMATIC)
[x] ACADEMY-11 : injection du matériel dans Moodle — **S-013 3/4 US + S-016 4/4 US** (domaine `academy.moodle`, tâche `generateMoodleImport` opt-in/dégradée, service compose one-shot `moodle-material` avec volume partagé + garde de rejeu, BDD 12 scénarios, dogfooding réel `compose up` via les outils natifs `moosh`/`moodle-blueprint`) — **EPIC TERMINÉ**
[x] ACADEMY-2 : brancher `ToolExposure` (N0) sur `OpenCodeCatalog` — **TERMINÉ S-013** (`OpenCodeCatalog.exposure()` + guide dérivé du contrat)
[x] ACADEMY-2/3 : implémenter les ports N0 — **CLOS côté academy S-013** (D-ACADEMY-11-1 : la boucle `FormationRuntimePort` appartient à l'étage privé ; `SessionMemoryContract` déjà implémenté par codex `PgVectorSessionMemory` S-094)
[x] ACADEMY-12-2 : writers `install.sh` unique (Linux apt / macOS Colima, queue commune verbatim) + `install.bat` (sonde admin+WSL2, WSL2/Desktop resumable) + port `EnvironmentProbe` + adapter `HostEnvironmentProbe` + `HostOs.fromOsName` + task `checkContainerEngine` (verdict rapporté) + functional TestKit + dogfooding Linux — **TERMINÉ S-015**
[ ] ACADEMY-12-3 : dogfooding environnement Windows réel (`install.bat`, `docker compose config`, parité, images tirées) — ne pas livrer sans preuve
[ ] ACADEMY-10 : migration du bridge Ktor → Spring Boot WebFlux (cadré S-012) — à ordonnancer sur décision pilote ; prérequis probable bump BOM workspace vers Spring Boot 4.1.1 (Jackson 3)
[ ] ACADEMY-9 : support Qualiopi (bilan de session, traces, conformité) — distinct de l'ingestion (ACADEMY-11)
[ ] Vérifier la conformité avec les décisions actées dans VISION_PIPELINE_FORMATION.adoc (sections 3.3, 9.2, 8.2) — **corrigée S-013** : § 6 (academy n'implémente pas la boucle)
[ ] Définir précisément les dépendances nécessaires pour les runtime-contracts (N0, quand ACADEMY-4/2/3)