= TEST_COVERAGE_ANALYSIS — academy-gradle

_Dernière mise à jour_ : 2026-09-21 (session 011)

== État des tests

Sessions 000-002 : aucune modification de code source (bootstrap, architecture, gouvernance).
Session 003 : implémentation EPIC ACADEMY-1 (socle générateur d'installateurs) — première vraie couverture de code.
Session 004 : révisions pilote du scaffold compose (postgres + ollama + portainer, parité Linux/Windows).
Session 005 : ACADEMY-6-4 terminé — compose complet (network, volumes nommés, healthcheck PostgreSQL, `.env`) + parité structurelle (source unique `composeBody()`/`envBody()`).
Session 006 : ACADEMY-6-5 terminé — seed DB data-only (service one-shot `moodle-seed`, entrypoint idempotent, SQL `WHERE NOT EXISTS`) + parité structurelle Linux/Windows (`seedScriptBody()`/`seedCourseSqlBody()`) + idempotence prouvée PostgreSQL 16 réel.
Session 007 : ACADEMY-6-6 terminé — dogfooding bout-en-bout (5 écarts de scaffold inexécutable corrigés).
Session 008 : ACADEMY-6-7 terminé — Cucumber BDD étendu (contenu OS, chemins, idempotence, erreurs, `opencode.json` différé) + **hôte = Docker uniquement (D-ACADEMY-6-10 appliqué : zéro provisioning Java/Gradle hôte)** ; ACADEMY-6-1/2/3 CADUQUES absorbés.
Session 009 : **ACADEMY-5 terminé** — exposition agent opencode. Domaine pur `academy.opencode` (`OpenCodeConfig`/`OpenCodePlugin`/`OpenCodeCatalog`/`OpenCodeConfigGenerator`/`LearnerGuideGenerator`/`WorkspaceDockerfileGenerator`), service `workspace` (profile `agent`), `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` générés (parité Linux/Windows), extension +3 properties. Les 2 scénarios « workspace/opencode différés » migrés en « générés ». Dogfooding réel : build image + `opencode 1.18.31` learner (bug install-root D-ACADEMY-5-11).
Session 010 : **ACADEMY-7 terminé** — BYOK LLM multi-provider. Domaine pur `academy.byok` (`ByokProviderSpec`/`ByokProviderCatalog`), `OpenCodeConfig` +2 champs, `OpenCodeConfigGenerator` multi-provider, `LearnerGuideGenerator` BYOK concret, extension +2 properties, `.env` nom-seul + compose transmet la variable. Dépendance N0 `runtime-contracts` (supersede D-ACADEMY-5-9). Dogfooding réel GEMINI + défaut.

## Ce qui est couvert

- **ACADEMY-1-0 (wiring)** : `AcademyPluginTest` (3 unit) — chargement du plugin, conventions d'extension par défaut, aggregation des 3 plateformes, surcharges (applicationName/outputDir).
- **ACADEMY-1-1 (domaine)** : `InstallerScriptGeneratorTest` (25 unit) — contenu Linux (pins toolchain documentaires, garde idempotence versionnée `~/.academy/.installed-`), scaffold compose conditionnel, **compose complet Linux (network `academy-net`, volumes nommés `postgres-data`/`ollama-models`/`portainer-data`, healthcheck `pg_isready`, `.env` + `POSTGRES_PASSWORD=` vide, Moodle `pgsql`)**, **seed DB data-only (service one-shot `moodle-seed`, entrypoint `poll mdl_course` + `psql -f seed-course.sql`, SQL `INSERT ... WHERE NOT EXISTS` sur `idnumber='academy-seed'`)** , convention credentials (prefix seulement, jamais de valeur), rendu Windows (admin, PowerShell, **escape cmd `2^>^&1`**), macOS (Homebrew, sans sudo), **hôte Docker-only sur les 3 OS (absence `JAVA_HOME`/`temurin`/`adoptium`/`services.gradle.org`/`setx JAVA_HOME`)**, **erreurs « Docker requis » + exit non-zéro**, **marqueur d'idempotence versionné**, **staging compose sous `$APP_DIR`**, **`opencode.json` différé (TODO ACADEMY-5)**, invariants de plateforme, composition du path relatif, **parité Windows du contrat compose + seed**.
- **ACADEMY-1-2 / 6-4 / 6-5 / 6-7 (intégration)** : `AcademyInstallerFunctionalTest` (6 TestKit) — `buildAllInstallers` écrit les 3 fichiers dans le layout attendu ; applicationName/outputDir custom honorés via TestKit Gradle runner ; **écriture réelle de `docker-compose.yml` + `.env` + `seed/seed.sh` + `seed/seed-course.sql` sur disque** (bash `cat >` et batch `> ... (`) avec network/volumes/healthcheck/seed assertés sur les deux plateformes ; **hôte Docker-only (aucun provisioning hôte dans les scripts générés)**.
- **BDD (pattern S-082)** : `cucumberTestInstaller` — feature `academy_installer.feature` `@installer` (21 scénarios : linux bash deterministe toolchain, compose only when enabled + postgres/ollama/portainer, compose complet Linux, **hôte Docker-only Linux/Windows/macOS**, **erreurs Docker requis**, **staging compose sous `$APP_DIR`**, **workspace généré (Dockerfile + opencode.json + AGENTS.md)**, credentials convention, windows batch admin + docker bootstrap, macos Homebrew sans sudo, parité Windows du contrat compose complet, **seed service one-shot, seed entrypoint idempotent, seed sql data-only, parité Windows des fichiers seed**). Steps domaine pur (zéro Gradle/IO). `cucumberTestOpenCode` — feature `academy_opencode.feature` `@opencode` (5 scénarios : config ollama, JSON balancé, pas de credential, guide inventoriant les tâches publiques, image basée `gradle:9.7.1-jdk25`). Steps domaine pur (`AcademyOpenCodeSteps`, pattern S-088).
- **ACADEMY-5-1 (domaine `academy.opencode`)** : 22 unit — `OpenCodeCatalogTest` (7 : namespace public, zéro borough privé, tâche par plugin, entry points formation, `tasksFor` inconnu = null, `allTasks` déterministe, invariants), `OpenCodeConfigGeneratorTest` (6 : schema+provider ollama, JSON balancé, déterminisme, modèle configurable, zéro credential, invariants), `LearnerGuideGeneratorTest` (4 : identité+workflow, inventaire catalogue, guardrail, zéro credential), `WorkspaceDockerfileGeneratorTest` (5 : base gradle pinnée, zsh+opencode+learner, **install après `USER learner` + `HOME`**, workdir+cmd, zéro credential).
- **ACADEMY-7-1 (domaine `academy.byok`)** : 21 unit — `ByokProviderSpecTest` (4 : identité, défauts null, invariants blank, invariants conditionnels), `ByokProviderCatalogTest` (7 : mapping total, ollama local sans clé, ollama-cloud endpoint+`OLLAMA_API_KEY`, gemini `@ai-sdk/google`+`GEMINI_API_KEY`, huggingface router+`HF_TOKEN`, custom, catalogue source unique), `OpenCodeConfigByokTest` (10 : **byte-identité ACADEMY-5 par défaut**, ollama-cloud, gemini sans baseURL, huggingface, custom, custom env var blank KO, custom baseUrl KO, JSON balancé pour tous les providers, **zéro valeur de clé pour tous les providers**), `LearnerGuideByokTest` (4 : runtime embarqué « no key », provider externe nomme la variable, custom env var, zéro valeur).
- **ACADEMY-7-2 (chaîne BYOK)** : 7 unit — `InstallerScriptByokTest` (runtime embarqué sans variable, provider externe documente le nom sans valeur, compose transmet la variable, custom nom fourni, **zéro valeur de clé pour tous les providers**, **parité Windows**, custom sans nom KO) ; `AcademyPluginTest` +2 assertions (défauts `openCodeProvider`/`openCodeApiKeyEnvVar`) ; `AcademyInstallerFunctionalTest` +2 TestKit (provider configuré → `.env`+`opencode.json` ; custom baseUrl + env var).
- **BDD (pattern S-082)** : `cucumberTestByok` — feature `academy_byok.feature` `@byok` (8 scénarios : défaut embarqué sans clé, ollama-cloud, gemini, huggingface, custom, JSON balancé, guide externe nomme la variable, guide embarqué « no key »). Glue préfixée `byok` (`AcademyByokSteps`, leçon S-088 anti-collision avec `AcademyOpenCodeSteps` partageant le même chemin de glue).
- **ACADEMY-8-1/2 (domaine `academy.bridge` + serveur Ktor)** : 23 unit — `MoodleEventTest` (3 : identité + payload opaque, payload vide par défaut, invariants), `BridgeEventTranslatorTest` (6 : prompt depuis l'identité, **déterminisme/session stable**, payload → contexte, payload vide → aucun contexte inventé, events distincts → sessions distinctes, type `SessionPrompt`), `BridgeRoutesTest` (4 : routes déclarées, méthode+description, GET/POST, table source unique), `BridgeServerConfigTest` (3 : identité, port hors plage KO, identité vide KO), `WebhookBridgeServerTest` (7 `testApplication` **sans port réel** : `/health` provider+model, `POST /events/moodle` ack+écho, `POST /session` ack, event malformé **400**, JSON invalide **400**, route inconnue **404**, **zéro fuite de credential**) ; `ServeWebhookBridgeTaskTest` (3 : config résolue, provider suivi, bridge désactivé par défaut) ; `AcademyPluginTest` (+1 tâche) ; `LearnerGuideByokTest` (+1 doc conditionnelle) ; `InstallerScriptByokTest` (+1 scaffold conditionnel).
- **ACADEMY-8-2 (opt-in, TestKit)** : `AcademyInstallerFunctionalTest` (+1) — `serveWebhookBridge` **SKIPPED** par défaut (un build n'ouvre jamais un port) — D-ACADEMY-8-7 prouvé fonctionnellement.
- **BDD (pattern S-082)** : `cucumberTestBridge` — feature `academy_bridge.feature` `@bridge` (6 scénarios : schéma academy + payload opaque, session stable, sessions distinctes, payload vide sans contexte, table de routes, **le prompt ne revendique aucun format tiers**). Glue préfixée `bridge` (`AcademyBridgeSteps`, S-088).

## Ce qui est à couvrir

- ACADEMY-2/3/4 (ports N0) et ACADEMY-9 (Qualiopi) : pas de tests tant que non implémentés.
- ACADEMY-6-1/2/3 : **CADUQUES (S-008)**.
- ACADEMY-5 : `ToolExposure` (N0) non encore consommé par le code (guardrail porté par `OpenCodeCatalog`) — à brancher en ACADEMY-2.
- Publication Maven Central d'academy : jamais faite (le plugin n'est pas publié).

## Évolution future

| Date | Tests | Résultat |
| 2026-09-19 | Aucun (session build/gouvernance) | N/A |
| 2026-09-20 | Aucun (session recadrage gouvernance) | N/A |
| 2026-09-20 | 10 unit + 2 functional + 5 cucumber (17 checks) | 0 FAIL — `check` VERT |
| 2026-09-22 | 12 unit (dont parité Windows) + 2 functional + 5 cucumber | 0 FAIL — `check` VERT |
| 2026-09-22 | 26 unit (dont compose complet Linux/Windows) + 3 functional (compose+env sur disque) + 14 cucumber (7 scénarios × 2 runners) | 0 FAIL — `check` VERT (43 checks) |
| 2026-09-22 | 16 unit + 4 functional (dont seed sur disque) + 11 scénarios cucumber (dont seed ×4) — RED prouvé (unit/cucumber/functional) | 0 FAIL — `check` VERT + `validatePlugins` VERT |
| 2026-09-21 | 21 unit + 5 functional + 15 scénarios cucumber (dogfooding 5 écarts) | 0 FAIL — `check` VERT + `validatePlugins` VERT |
| 2026-09-21 | 28 unit + 6 functional + 21 scénarios cucumber (hôte Docker-only, chemins, erreurs, opencode.json différé) — RED prouvé aux 3 niveaux (unit 6 failed / cucumber 6 failed / functional 1 failed) | 0 FAIL — `check` VERT + `validatePlugins` VERT + `publishToMavenLocal` VERT |
| 2026-09-21 | **50 unit + 7 functional + 21 scénarios installer + 5 scénarios opencode** (exposition agent : domaine `academy.opencode`, workspace généré) — RED prouvé par échec de compilation | 0 FAIL — `check` VERT + `validatePlugins` VERT + `publishToMavenLocal` VERT + dogfooding `docker build` + `opencode 1.18.31` |
| 2026-09-21 | **218 unit + 9 functional + 21 installer + 5 opencode + 8 byok scénarios** (BYOK multi-provider : domaine `academy.byok`, générateur multi-provider, chaîne `.env`→compose→`{env:NAME}`) — RED prouvé par échec de compilation (N0 puis domaine puis wiring) | 0 FAIL — `check` VERT + `validatePlugins` VERT + `publishToMavenLocal` VERT + dogfooding GEMINI/défaut (clé absente du JSON, byte-identité ACADEMY-5) |
| 2026-09-21 | **311 unit + 10 functional + 21 installer + 5 opencode + 8 byok + 6 bridge scénarios** (bridge Ktor : domaine `academy.bridge`, serveur `testApplication`, tâche opt-in SKIPPED, anti-corruption `BridgeWire`) — RED prouvé par échec de compilation (dépendance → domaine → wiring) + RED de **comportement** (sérialisation N0) | 0 FAIL — `check` VERT + `validatePlugins` VERT + `publishToMavenLocal` VERT + dogfooding réel serveur + curl (`/health` 200, ack, 400/404) |
|===