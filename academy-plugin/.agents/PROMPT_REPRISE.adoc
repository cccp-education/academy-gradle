= PROMPT_REPRISE — academy-gradle

== Mission de la prochaine session

EPIC ACADEMY-8 **TERMINÉ** (S-011). Passer à **ACADEMY-4** —
MaterialUpdateContract (liaison avec le bureau pour recevoir le matériel
versionné). Le mécanisme N0 acté (vision § 3.2 / contrat `MaterialUpdateContract`
+ `MaterialUpdateResolver`) est **git pull** : l'apprenant **tire** des versions,
le créateur ne pousse pas. Vérifier la source avant de nommer
(`runtime-contracts` est sur le classpath depuis ACADEMY-7).

Ordre suggéré ensuite : ACADEMY-2/3 (FormationRuntimePort / SessionMemoryContract),
puis ACADEMY-9 (Qualiopi).

== État de l'architecture (session 011)

- **ACADEMY-1 TERMINÉ** : socle générateur (extension `academyInstaller`,
  domaine `installer`, 4 tâches).
- **ACADEMY-6 TERMINÉ (S-008)** : installeur hybride multiplateforme (compose
  Moodle/PostgreSQL/Ollama/Portainer + seed data-only, hôte = Docker only).
- **ACADEMY-5 TERMINÉ (S-009)** : exposition agent (`opencode.json`,
  `AGENTS.md`, service `workspace` profile `agent`, image générée).
- **ACADEMY-7 TERMINÉ (S-010)** : BYOK multi-provider — domaine `academy.byok`
  (`ByokProviderSpec`/`ByokProviderCatalog`), `{env:NAME}` jamais la valeur,
  `.env` nom-seul + compose transmet, `runtime-contracts` N0.
- **ACADEMY-8 TERMINÉ (S-011)** : bridge Ktor local (décision pilote — Ktor
  acté).
  - Domaine pur `education.cccp.academy.bridge` : `MoodleEvent` (schéma
    **academy**, pas un format Moodle tiers), `BridgeEventTranslator` (→
    `SessionPrompt` N0, session id **stable**), `BridgeRoutes` (`/health`,
    `/events/moodle`, `/session`), `BridgeServerConfig`, `BridgeWire`
    (anti-corruption — les contrats N0 ne sont **pas** `@Serializable`),
    `ByokBridgeSupport`.
  - Serveur `WebhookBridgeServer` (`installBridge` module unique test+prod,
    `startBridge` Netty daemon).
  - Tâche `serveWebhookBridge` **opt-in** (`bridgeEnabled` défaut `false`,
    `onlyIf`), extension +3 properties (`bridgeHost` `127.0.0.1`,
    `bridgePort` `8765`), `AGENTS.md` documente le bridge seulement si activé.
  - Le bridge **route et acquitte — il ne pense pas** (zéro LLM).
  - Ktor 3.2.0 + kotlinx-serialization + `opencode-session-contracts` en
    `implementation`.
- Gates S-011 : `clean check validatePlugins` VERT **311 unit + 10 functional +
  21 installer + 5 opencode + 8 byok + 6 bridge**, `publishToMavenLocal` VERT,
  zéro secret, dogfooding réel (serveur + curl : `/health` 200,
  `POST /events/moodle` ack, 400/404).

== Points en attente / ordre de travail suggéré

1. ACADEMY-4 : MaterialUpdateContract (git pull). Cadrer d'abord (pattern S-057).
2. ACADEMY-2/3 : ports N0 (FormationRuntimePort / SessionMemoryContract).
3. `ToolExposure` (N0) toujours non consommé (guardrail porté par
   `OpenCodeCatalog`) — à brancher en ACADEMY-2.
4. ACADEMY-9 : Qualiopi (bilan de session, traces, conformité).
5. Publication Maven Central d'academy : jamais faite — à planifier sur demande.

## Rappel des ordres de mission

- BDD/TDD baby-step strict : RED → GREEN → REFACTOR, 1 fichier de test avant le code.
- Gouvernance d'abord pour toute décision pilote (pattern S-057).
- Parité cross-plateforme : tout service/contenu transverse est asserté Linux ET
  Windows — **préférer une source unique partagée plutôt qu'un doublon asserté**.
- DAG N2 : academy importable par N3, jamais N3 → academy. Academy ne consomme
  pas les tâches N1 (le bridge stdin/stdout de codebase n'est **pas** réutilisé —
  D-ACADEMY-8-1).
- Zéro secret dans le code. `publishToMavenLocal` obligatoire après toute modification de code source.
- Code anglais only (OSS cccp-education), docs gouvernance en français.
- **Dogfooding = preuve réelle** : un test vert ne prouve pas qu'un scaffold
  s'exécute (S-007 `moodle:4.5`, S-009 install opencode en root, S-010 non-fuite
  de la clé, S-011 serveur réel + curl).
- **Ne jamais inventer un nom d'image, de tâche, de provider ou de plugin** :
  vérifier la source / le registre avant de l'écrire (models.dev + docs opencode
  pour ACADEMY-7 ; Central pour Ktor 3.2.0 en ACADEMY-8).
- **Vérifier la source avant de citer un précédent** : la justification
  « le bridge opencode existe déjà » (vision D4) était fausse (stdin/stdout ≠
  HTTP) — leçon S-011.
</content>
