= PROMPT_REPRISE — academy-gradle

== Mission de la prochaine session

EPIC ACADEMY-5 **TERMINÉ** (S-009). Passer à **ACADEMY-7** — BYOK LLM :
configuration du fournisseur LLM réel (LlmProviderKind OLLAMA_LOCAL /
OLLAMA_CLOUD / GEMINI / HUGGINGFACE / CUSTOM) alimentant le `.env` **par
convention, jamais la valeur**, et le `provider` du `opencode.json` généré.

**Contient le TODO laissé S-009** : `opencode.json` câble aujourd'hui le seul
provider `ollama` embarqué (`http://ollama:11434/v1`, modèle
`gpt-oss:120b-cloud`). ACADEMY-7 doit le rendre **multi-provider** depuis
`ByokLlmConfig` (runtime-contracts N0) — provider + modèle + clé par env var.

== État de l'architecture (session 009)

- **ACADEMY-1 TERMINÉ** : socle générateur (extension `academyInstaller`,
  domaine `education.cccp.academy.installer`, 4 tâches).
- **ACADEMY-6 TERMINÉ (S-008)** : installeur hybride multiplateforme
  (compose Moodle/PostgreSQL/Ollama/Portainer + seed data-only, hôte = Docker
  uniquement).
- **ACADEMY-5 TERMINÉ (S-009)** : exposition agent.
  - Domaine pur `education.cccp.academy.opencode` (6 fichiers) :
    `OpenCodeConfig`, `OpenCodePlugin`, `OpenCodeCatalog` (11 plugins publics),
    `OpenCodeConfigGenerator` (JSON zéro-lib), `LearnerGuideGenerator`
    (`AGENTS.md`), `WorkspaceDockerfileGenerator`.
  - Service `workspace` dans `composeBody()` (`build: ./project`, image
    `cccp-education/academy-workspace:latest`, profile `agent`, opt-in
    `docker compose --profile agent up`) — **le TODO S-007 est résolu**.
  - `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` générés,
    parité structurelle Linux/Windows.
  - Extension +3 properties : `openCodeEnabled` (défaut true),
    `openCodeModel` (`gpt-oss:120b-cloud`), `openCodeProviderUrl`
    (`http://ollama:11434/v1`).
  - **Dogfooding réel** : Dockerfile buildé, `opencode 1.18.31` en tant que
    `learner`, compose/jq/sh -n VALIDES. Bug attrapé : install opencode en
    root → `/root/.opencode` invisible au learner (D-ACADEMY-5-11 corrigée).
- Gates S-009 : `clean check validatePlugins` VERT **50 unit + 7 functional +
  21 scénarios installer + 5 scénarios opencode**, `publishToMavenLocal` VERT,
  zéro secret.

== Points en attente / ordre de travail suggéré

1. ACADEMY-7 : `ByokLlmConfig` → `opencode.json` multi-provider. Cadrer d'abord
   (pattern S-057) : comment la clé (jamais la valeur) est référencée, mapping
   `LlmProviderKind` → provider opencode, provider par défaut sans BYOK.
2. ACADEMY-8 (webhooks Moodle + bridge Ktor) après ACADEMY-7.
3. ACADEMY-2/3/4 (FormationRuntimePort / SessionMemoryContract /
   MaterialUpdateContract) : l'implémentation des ports N0, séquencée après la
   machine complète (elle consomme `runtime-contracts`, déjà publié Central
   0.0.1, BOM MEMPHIS 0.0.47+). Zéro bump BOM requis à ce stade.
4. ACADEMY-5 reste **ouverte documentairement** : `ToolExposure` (N0) n'est pas
   encore consommé par le code (le guardrail est porté par `OpenCodeCatalog`) —
   à brancher en ACADEMY-2.

## Rappel des ordres de mission

- BDD/TDD baby-step strict : RED → GREEN → REFACTOR, 1 fichier de test avant le code.
- Gouvernance d'abord pour toute décision pilote (pattern S-057).
- Parité cross-plateforme : tout service/contenu transverse est asserté Linux ET
  Windows — **préférer une source unique partagée plutôt qu'un doublon asserté**.
- DAG N2 : academy importable par N3, jamais N3 → academy.
- Zéro secret dans le code. `publishToMavenLocal` obligatoire après toute modification de code source.
- Code anglais only (OSS cccp-education), docs gouvernance en français.
- **Dogfooding = preuve réelle** : un test vert ne prouve pas qu'un scaffold
  s'exécute (S-007 `moodle:4.5` inexistant, S-009 install opencode en root).
- **Ne jamais inventer un nom d'image, de tâche ou de plugin** : vérifier la
  source / le registre avant de l'écrire (audit systématique en ouverture).
</content>
