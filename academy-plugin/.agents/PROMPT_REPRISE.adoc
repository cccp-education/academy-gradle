= PROMPT_REPRISE — academy-gradle

== Mission de la prochaine session

EPIC ACADEMY-6 **TERMINÉ** (S-008). Passer à **ACADEMY-5** — Intégration du
contrat InteractionProtocol : `opencode.json` + session zsh dans le conteneur
workspace, exposition des tâches Gradle `education.cccp`.

**Contient le TODO laissé par S-007** : publier
`cccp-education/academy-workspace:latest` puis réinsérer le service
`workspace` dans `composeBody()` (point d'insertion marqué
`# TODO ACADEMY-5`).

== État de l'architecture (session 008)

- **ACADEMY-1 TERMINÉ (3/3 US)** : socle générateur. Extension
  `academyInstaller` Property-based, domaine pur
  `education.cccp.academy.installer`, tâches generateLinux/Windows/MacInstaller
  + buildAllInstallers.
- **ACADEMY-6 TERMINÉ** (6-0 cadrage, 6-4 compose, 6-5 seed, 6-6 dogfooding,
  6-7 BDD étendu ; **6-1/2/3 CADUQUES** absorbés par 6-7) :
  - compose complet (network `academy-net`, volumes nommés, healthcheck
    PostgreSQL, `.env`), parité **structurelle** Linux/Windows via
    `composeBody()`/`envBody()`.
  - seed DB data-only, service one-shot `moodle-seed`, SQL
    `INSERT ... WHERE NOT EXISTS`.
  - **hôte = Docker uniquement (D-ACADEMY-6-10 appliqué S-008)** : les 3
    scripts ne provisionnent plus Java Temurin ni Gradle ; `JAVA_VERSION`/
    `GRADLE_VERSION` restent des pins documentaires de l'image workspace.
  - dogfooding bout-en-bout (S-007) : Moodle 5.2.3 `/login` HTTP 200, seed
    idempotent prouvé.
- Gates S-008 : `clean check validatePlugins` VERT **28 unit + 21 scénarios
  Cucumber + 6 functional**, `publishToMavenLocal` VERT, zéro secret,
  zéro `mariadb`, dogfooding `sh -n` + `docker compose config` VALIDES.

== Points en attente / ordre de travail suggéré

1. ACADEMY-5 : `opencode.json` + session zsh ; réinsérer le service
   `workspace` dans `composeBody()` une fois l'image publiée. Cadrer d'abord
   (pattern S-057) : quel contenu d'`opencode.json`, comment il expose les
   tâches Gradle `education.cccp`.
2. ACADEMY-7 (BYOK LLM) après ACADEMY-5 : fournisseur (OLLAMA_LOCAL,
   OLLAMA_CLOUD, GEMINI, HUGGINGFACE, CUSTOM) alimentant le `.env` par
   convention (jamais de valeur).
3. ACADEMY-4/2/3 (contrats MaterialUpdate / FormationRuntimePort /
   SessionMemoryContract) séquencés après la machine fonctionnelle.

## Rappel des ordres de mission

- BDD/TDD baby-step strict : RED → GREEN → REFACTOR, 1 fichier de test avant le code.
- Gouvernance d'abord pour toute décision pilote (pattern S-057).
- Parité cross-plateforme : tout service/contenu transverse est asserté Linux ET
  Windows — **préférer une source unique partagée plutôt qu'un doublon asserté**.
- DAG N2 : academy importable par N3, jamais N3 → academy.
- Zéro secret dans le code. `publishToMavenLocal` obligatoire après toute modification de code source.
- Code anglais only (OSS cccp-education), docs gouvernance en français.
- **Dogfooding = preuve réelle** : un test vert ne prouve pas qu'un scaffold
  s'exécute (S-007 l'a démontré — `moodle:4.5` passait les 43 checks et était
  inexistant).
</content>
