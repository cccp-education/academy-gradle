= PROMPT_REPRISE — academy-gradle

== Mission de la prochaine session

Poursuivre l'EPIC ACADEMY-6 (installeur hybride) — ordre acté **1 → 6 → 5 → 7**.
US suivante : **ACADEMY-6-5** — seed DB **data-only** au premier `compose up`
(entrypoint idempotent, import d'une course Moodle minimale). Ensuite
ACADEMY-6-6 (dogfooding conteneur) puis ACADEMY-6-7 (Cucumber étendu).

Contexte :
- Le borough Bagdad (academy-gradle) livre une **machine d'expérimentation
  complète du pipeline cccp.education** (vision actée S-002, D-ACADEMY-1..4).
- Ressource = hôte (**Docker UNIQUEMENT**, D-ACADEMY-6-10) + Docker (Moodle +
  PostgreSQL + Ollama + Portainer + workspace opencode zsh).
- L'apprenant consomme les plugins education.cccp (bakery, document, slider,
  capsule…) via Maven Central + Gradle, zéro VPS, mono-utilisateur.

== État de l'architecture (session 005)

- **ACADEMY-1 TERMINÉ (3/3 US)** : socle générateur. Extension
  `academyInstaller` Property-based, domaine pur
  `education.cccp.academy.installer`, tâches generateLinux/Windows/MacInstaller
  + buildAllInstallers.
- **ACADEMY-6-0 cadrage** → `.agents/CADRAGE_EPIC_ACADEMY_INSTALLER.adoc`
  (constats, décisions **D-ACADEMY-6-1..13**, US 6-1..7, options écartées).
- **ACADEMY-6-4 TERMINÉ (S-005)** : `docker-compose.yml` **complet et exécutable**
  — network dédié `academy-net`, volumes nommés `postgres-data`/`ollama-models`/
  `portainer-data`, healthcheck PostgreSQL `pg_isready` + `depends_on`
  `service_healthy`, Moodle câblé `pgsql`, `.env` généré (noms seulement, zéro
  valeur). **Parité structurelle** Linux/macOS + Windows : `composeBody()`/
  `envBody()` = source unique partagée par le heredoc bash et le writer batch.
  Dogfooding : `docker compose config` **VALIDE** (YAML réel exécutable).
- Gates S-005 : `clean check` VERT **43 checks** (26 unit + 14 cucumber + 3
  functional) 0 FAIL, `validatePlugins` VERT, `publishToMavenLocal` VERT,
  zéro secret, zéro `mariadb` en prod.

== Points en attente / ordre de travail suggéré

1. ACADEMY-6-5 : seed DB data-only (entrypoint idempotent, course minimale) —
   brancher dans le compose généré (`composeBody()`), zéro migration destructive.
2. ACADEMY-6-6 (dogfooding conteneur éphémère, installeur Linux réel,
   étapes 1→7 — validation manuelle documentée) puis ACADEMY-6-7 (Cucumber BDD
   étendu : contenu OS, compose, idempotence, erreurs).
3. ACADEMY-6-1/2/3 (raffermissement Linux/Windows/macOS) : **partiellement
   caducs** pour le provisioning hôte Java/Gradle (D-ACADEMY-6-10) — recadrer
   avant implémentation.
4. Ensuite ACADEMY-5 (opencode.json + session zsh) puis ACADEMY-7 (BYOK LLM).

## Rappel des ordres de mission

- BDD/TDD baby-step strict : RED → GREEN → REFACTOR, 1 fichier de test avant le code.
- Gouvernance d'abord pour toute décision pilote (pattern S-057).
- Parité cross-plateforme : tout service compose transverse est asserté Linux ET
  Windows — **préférer une source unique partagée plutôt qu'un doublon asserté**.
- DAG N2 : academy importable par N3, jamais N3 → academy.
- Zéro secret dans le code. `publishToMavenLocal` obligatoire après toute modification de code source.
- Code anglais only (OSS cccp-education), docs gouvernance en français.
