= INDEX — Academy Gradle
:toc:
:toclevels: 2

_Regles absolues_ : voir `AGENT.adoc`
_DAG_ : N2 — Plugin Gradle de consommation (Regle 4bis)
_Projet_ : academy-gradle (education.cccp.academy)

_Derniere mise a jour_ : 2026-09-25 (session 016)
_Session terminee_ : 016 — **ACADEMY-11-4 TERMINÉ + EPIC ACADEMY-11 CLOS 4/4 US** (service compose one-shot `moodle-material`) : un service frère de `moodle-seed` (`restart: "no"`, `entrypoint`, `depends_on: moodle: service_healthy`) **applique** le plan stagé après le schéma Moodle, en pilotant les outils natifs de l'image. **Volume nommé `moodle-html:/var/www/html`** partagé par `moodle` et `moodle-material` — indispensable car `moosh` bootstrappe contre l'install (config.php généré au boot). **Garde de rejeu** `sha256(plan.json)` → marqueur `.academy-applied-plan` dans le volume inscriptible (`moosh activity-add` **ne dédoublonne pas** — bug réel corrigé). **Input tolérant** `GenerateInstallerTask.moodleMaterialFiles` (`@InputFiles @Optional`, pattern S-221) : un re-run simple ré-rend un plan changé (bug réel de staleness corrigé). Lecture matériel extraite/partagée (`MoodleMaterialReader`). **Dogfooding réel `compose up`** : cours `formation-fpa` + sections + activités, contenu AsciiDoc présent en base (`mdl_page.content`/`mdl_label.intro`), rejeu no-op prouvé. Gates **710 unit + 21 functional + 71 scénarios Cucumber** 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT, zéro secret
_Sessions precedent_ : 015 — ACADEMY-12-2 TERMINÉ (writers portables + sonde adapter + dogfooding Linux) ; 014 — ACADEMY-12 cadré + ACADEMY-12-1 TERMINÉ ; 013 — ACADEMY-11 3/4 US (injection matériel Moodle, outils natifs `moosh`/`moodle-blueprint`, dogfooding réel) + ToolExposure N0 ; 012 — ACADEMY-4 TERMINÉ + publication Maven Central 0.0.1 ; 011 — ACADEMY-8 TERMINÉ (bridge Ktor) ; 010 — ACADEMY-7 TERMINÉ (BYOK multi-provider) ; 009 — ACADEMY-5 TERMINÉ (exposition agent) ; 008 — ACADEMY-6-7 TERMINÉ + EPIC ACADEMY-6 TERMINÉ ; 007 — ACADEMY-6-6 TERMINÉ (dogfooding bout-en-bout) ; 006 — ACADEMY-6-5 TERMINÉ (seed DB data-only) ; 005 — ACADEMY-6-4 TERMINÉ (compose complet) ; 004 — Révisions pilote du scaffold compose ; 003 — Socle générateur (ACADEMY-1) + cadrage ACADEMY-6-0 ; 002 — Recadrage de l'orientation ; 001 — Alignement article 0124 ; 000 — Bootstrap
_Prochaine session_ : à ordonnancer sur décision pilote — **ACADEMY-12-3** (dogfooding Windows réel, exige un hôte Windows), **ACADEMY-10** (migration Ktor → Spring Boot WebFlux, cadrage + bump BOM), **ACADEMY-9** (Qualiopi, cadrage), **BUREAU-3** (autre borough). Aucun item restant n'est coddable localement sans prérequis externe.

'''
== REGLES ABSOLUES

=== 1. COMMITS/GIT — INTERDICTION FORMELLE sans permission
=== 2. PUBLISH TO MAVEN LOCAL — OBLIGATOIRE après toute modification de code source
=== 3. DAG N2 — importable par N3 runner-gradle, jamais importe N3
=== 4. PROCEDURE DE FIN DE SESSION — 6 ETAPES OBLIGATOIRES
=== 5. HOOK OUVERTURE — Checklist SESSION_CHECKLIST.adoc obligatoire

'''
== ✅ EPIC ACADEMY-0 — Bootstrap academy-gradle

_Source_ : Session 000. Initialisation du borough academe-gradle : création des fichiers de gouvernance, de la structure du plugin, et du placeholder de code.

_Dependances_ : Aucune.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-0-0
| *Bootstrap academy-gradle* — création du répertoire, des fichiers de gouvernance (AGENT.adoc, SESSION_CHECKLIST.adoc, INDEX.adoc, PROMPT_REPRISE.adoc), des fichiers de build (settings.gradle.kts, build.gradle.kts), et du code source placeholder (AcademyPlugin.kt).
| 2
| ✅ TERMINÉ (S-000)
|===

_Total_ : 2 US, ~2 pts. _Methode_ : baby-step DDD/TDD/BDD, 1 niveau (seulement le bootstrap).
_Statut_ : 1/1 US terminées (S-000 cadrage + bootstrap). EPIC ACADEMY-0 CLOS.

'''
== ✅ EPIC ACADEMY-0-BIS — Mise en conformité architecture 0124 + gouvernance + repo

_Source_ : Session 001. Alignement du borough sur l'architecture « Plugin Indépendant + Racine Consommateur » (article 0124), fusion de la gouvernance dans `academy-plugin/.agents/`, ajout de la licence Apache 2.0, préparation du dépôt GitHub cccp-education.

_Dependances_ : Aucune.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-0-BIS-0
| *Fusion gouvernance* — déplacement de AGENT.adoc, BACKLOG.adoc, INDEX.adoc, PROMPT_REPRISE.adoc, SESSION_CHECKLIST.adoc, SESSION_PROCEDURE.adoc, sessions/000-bootstrap.adoc de la racine vers academy-plugin/.agents/, suppression du .agents racine. git mv 100% rename.
| 2
| ✅ TERMINÉ (S-001)
| ACADEMY-0-BIS-1
| *Alignement article 0124* — .gitignore + untrack artefacts, wrappers Gradle 9.7.1 (racine + plugin), catalogue consommateur racine, settings/build racine consommateur (zéro include), catalogue complet plugin, settings plugin autonome, build plugin pattern bureau (toolchain 25, functionalTest, cucumberTest, signing, POM Apache 2.0).
| 3
| ✅ TERMINÉ (S-001)
| ACADEMY-0-BIS-2
| *Licence + repo* — LICENSE Apache 2.0 ajoutée ; vérification du dépôt GitHub cccp-education/academy-gradle (créé, vide, public, origin configuré).
| 1
| ✅ TERMINÉ (S-001)
|===

_Total_ : 3 US, ~6 pts. _Methode_ : restructuration build + gouvernance pure (pattern S-057/S-063).
_Statut_ : 3/3 US terminées (S-001). EPIC ACADEMY-0-BIS CLOS. Push vers origin en attente (décision pilote).

'''
== ✅ EPIC ACADEMY-0-TER (S-002) — Recadrage de l'orientation Academy (gouvernance pure)

_Source_ : Session 002 (arbitrage pilote). La ressource academy = **machine d'expérimentation complète du pipeline cccp.education** : hôte = Docker + Java Temurin 25 + Gradle 9.7.1 ; Docker = Moodle + MariaDB + conteneur workspace opencode en session zsh. Installeur par plateforme (sh/bat) sans VPS, mono-utilisateur.

_⚠ Révisé S-004_ : hôte = Docker uniquement (D-6-10) ; Docker = Moodle + **PostgreSQL** + **Ollama** + **Portainer** + workspace (D-6-11/12/13).

_Dependances_ : Aucune.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-0-TER-0
| *Recadrage BACKLOG* — vision hybride actée (D-ACADEMY-1..4), ACADEMY-1 pivoté vers socle générateur, ACADEMY-6 révisé en installeur hybride (US-0 ✅), ordre des Épics réordonné (produit → contrats).
| 2
| ✅ TERMINÉ (S-002)
| ACADEMY-0-TER-1
| *Suppression fichiers hors-projet* — files/ + files.zip (proposition externe `com.example.installer`) supprimés du working tree.
| 1
| ✅ TERMINÉ (S-002)
|===

_Total_ : 2 US, ~3 pts. _Methode_ : gouvernance pure.
_Statut_ : 2/2 US terminées (S-002). EPIC ACADEMY-0-TER CLOS.

'''
== ✅ EPIC ACADEMY-1 — Socle générateur d'installateurs

_Source_ : Session 003. Extension typée `academyInstaller` (Property-based), domaine pur `education.cccp.academy.installer`, tâches de génération par plateforme + agrégat, tests unit + functional + Cucumber BDD (runner dédié S-082).

_Dependances_ : Aucune.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-1-0
| *Extension + plugin wiring* — `AcademyInstallerExtension` (8 properties), `AcademyPlugin.wireDefaults` (conventions, applicationVersion := project.version), registration des 4 tâches.
| 3
| ✅ TERMINÉ (S-003)
| ACADEMY-1-1
| *Domaine pur installateur* — TargetOs / InstallerPlatform (+ invariants) / InstallerFile / InstallerScriptGenerator.render() (scripts bash/bat déterministes, zéro I/O).
| 3
| ✅ TERMINÉ (S-003)
| ACADEMY-1-2
| *Tests* — 3 unit plugin + 7 unit generator + 2 functional TestKit + 5 scénarios cucumber `academy_installer.feature` (runner `AcademyInstallerCucumberRunner`, `cucumberTestInstaller` branché à check).
| 3
| ✅ TERMINÉ (S-003)
|===

_Total_ : 3 US, ~9 pts. _Methode_ : baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR).
_Statut_ : 3/3 US terminées (S-003). EPIC ACADEMY-1 CLOS. Gates : check VERT (10 unit + 2 functional + 5 cucumber), validatePlugins VERT, publishToMavenLocal VERT.

'''
== ✅ EPIC ACADEMY-6 — Installeur hybride multiplateforme (machine d'expérimentation)

_Source_ : Sessions 003 (cadrage) + 004 (volet compose). Générateur de scripts
d'installation par plateforme + scaffold `docker-compose.yml` embarqué.

_Dependances_ : ACADEMY-1 (socle générateur) ✅.

_Révisions pilote actées S-004_ : D-ACADEMY-6-10 (hôte = Docker uniquement),
D-ACADEMY-6-11 (Ollama embarqué), D-ACADEMY-6-12 (Portainer remplace Docker
Desktop, transverse Windows), D-ACADEMY-6-13 (PostgreSQL remplace MariaDB).

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-6-0
| *Cadrage* — `CADRAGE_EPIC_ACADEMY_INSTALLER.adoc` (constats, D-ACADEMY-6-1..13, US 6-1..7, options écartées).
| 8
| ✅ TERMINÉ (S-003)
| ACADEMY-6-4
| *Scaffold compose* — Moodle + **PostgreSQL** + **Ollama** + **Portainer** + workspace opencode zsh, parité Linux/macOS + Windows. Network dédié, volumes nommés, healthcheck PostgreSQL, `.env` documenté livrés (S-005).
| 5
| ✅ TERMINÉ (S-005)
| ACADEMY-6-5
| *Seed DB data-only* — service one-shot `moodle-seed`, entrypoint idempotent (`poll mdl_course` puis `psql -f seed-course.sql`), SQL `INSERT ... WHERE NOT EXISTS` sur `idnumber='academy-seed'` (zéro DDL/DELETE/UPDATE), parité structurelle Linux/Windows (S-006).
| 5
| ✅ TERMINÉ (S-006)
| ACADEMY-6-1/2/3
| *Raffermissement Linux/Windows/macOS* — **CADUQUES (S-008)** : absorbés par ACADEMY-6-7 une fois D-ACADEMY-6-10 appliqué (hôte = Docker only, zéro provisioning Java/Gradle hôte à raffermir).
| —
| ✅ CADUQUE (S-008)
| ACADEMY-6-6
| *Dogfooding* conteneur éphémère, installeur Linux réel, étapes 1→7 (validation manuelle).
| —
| ✅ TERMINÉ (S-007)
| ACADEMY-6-7
| *Cucumber étendu* — contenu OS (hôte Docker-only), chemins, idempotence, erreurs, opencode.json différé + alignement D-ACADEMY-6-10.
| —
| ✅ TERMINÉ (S-008)
|===

_Statut_ : **EPIC ACADEMY-6 TERMINÉ (S-008)** — ACADEMY-6-0 ✅ + **ACADEMY-6-4 ✅ S-005** (compose complet, corrigé S-007) + **ACADEMY-6-5 ✅ S-006** (seed DB data-only) + **ACADEMY-6-6 ✅ S-007** (dogfooding bout-en-bout) + **ACADEMY-6-7 ✅ S-008** (BDD étendu + hôte Docker-only) ; **ACADEMY-6-1/2/3 ✅ CADUQUES S-008** (absorbés par 6-7).

_Gates S-008_ : `clean check validatePlugins` VERT (28 unit + 6 functional + 21 scénarios Cucumber), `publishToMavenLocal` VERT, dogfooding `sh -n` + `docker compose config` VALIDES, zéro provisioning hôte (grep 0), zéro secret, zéro `mariadb` en prod.

'''
== ✅ EPIC ACADEMY-5 — Intégration du contrat InteractionProtocol (opencode)

_Source_ : Session 009. Exposition de l'agent opencode au stagiaire : `opencode.json` + `AGENTS.md` générés, session zsh dans le conteneur `workspace` (image générée sur `gradle:9.7.1-jdk25`), inventaire des tâches Gradle `education.cccp` publiques.

_Dependances_ : ACADEMY-1 (socle) ✅, ACADEMY-6 (compose/seed) ✅.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-5-0
| *Cadrage* — `CADRAGE_EPIC_ACADEMY_5_OPENCODE.adoc` (constats, D-ACADEMY-5-1..11, options écartées). Découverte : l'image officielle `ghcr.io/anomalyco/opencode` est Alpine-minimal (inapte Gradle) → Dockerfile généré sur `gradle:9.7.1-jdk25`.
| 3
| ✅ TERMINÉ (S-009)
| ACADEMY-5-1
| *Domaine pur + wiring* — domaine `academy.opencode` (6 fichiers), extension 3 properties, service `workspace` (profile `agent`), `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` (parité Linux/Windows). Dogfooding réel : build image + `opencode 1.18.31` en tant que `learner` ; bug install-root corrigé (D-ACADEMY-5-11).
| 8
| ✅ TERMINÉ (S-009)
| ACADEMY-5-2
| *Provision du projet consommateur* — **absorbée par 5-1** : le `project/` généré (Dockerfile + opencode.json + AGENTS.md + README du premier `gradle build`) est le projet consommateur.
| —
| ✅ ABSORBÉE (S-009)
|===

_Total_ : 2 US, ~11 pts. _Methode_ : baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR).
_Statut_ : **EPIC ACADEMY-5 TERMINÉ (S-009)** — ACADEMY-5-0 ✅ + ACADEMY-5-1 ✅ + ACADEMY-5-2 ✅ absorbée.

_Gates S-009_ : `clean check validatePlugins` VERT (50 unit + 7 functional + 21 scénarios installer + 5 scénarios opencode), `publishToMavenLocal` VERT, dogfooding `sh -n` + `docker compose config` + `jq` + `docker build` VALIDES, `opencode --version` en tant que `learner`, zéro secret.

'''

== ✅ EPIC ACADEMY-7 — BYOK LLM multi-provider

_Source_ : Session 010. Configuration du fournisseur LLM réel (contrat N0
`LlmProviderKind`) alimentant `opencode.json`, la clé référencée **par le nom
de sa variable d'environnement** (`{env:NAME}`, interpolation officielle
opencode), jamais la valeur.

_Dependances_ : ACADEMY-5 (exposition agent) ✅, `runtime-contracts:0.0.1` (N0, Maven Central) ✅.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-7-0
| *Cadrage* — `CADRAGE_EPIC_ACADEMY_7_BYOK.adoc` (constats, D-ACADEMY-7-1..10, mapping provider vérifié, options écartées). Supersede D-ACADEMY-5-9 (dépendance N0 désormais assumée).
| 3
| ✅ TERMINÉ (S-010)
| ACADEMY-7-1
| *Domaine pur `academy.byok` + générateur multi-provider* — `ByokProviderSpec`/`ByokProviderCatalog` (mapping total vérifié S-010), `OpenCodeConfig` +2 champs, `OpenCodeConfigGenerator` multi-provider, `LearnerGuideGenerator` BYOK concret, dépendance N0.
| 5
| ✅ TERMINÉ (S-010)
| ACADEMY-7-2
| *Wiring + chaîne BYOK* — extension +2 properties, `InstallerPlatform`/`GenerateInstallerTask`, `.env` nom-seul, compose transmet la variable au service `workspace`, parité Linux/Windows.
| 3
| ✅ TERMINÉ (S-010)
| ACADEMY-7-3
| *BDD + dogfooding* — `academy_byok.feature` 8 scénarios `@byok` + runner dédié + glue préfixée (S-088) ; dogfooding réel GEMINI + défaut (clé absente du JSON, `compose config`/`jq`/`sh -n` VALIDES).
| 3
| ✅ TERMINÉ (S-010)
|===

_Total_ : 4 US, ~14 pts. _Methode_ : baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR).

_Statut_ : **EPIC ACADEMY-7 TERMINÉ (S-010)** — 7-0 ✅ + 7-1 ✅ + 7-2 ✅ + 7-3 ✅.

_Gates S-010_ : `clean check validatePlugins` VERT (218 unit + 9 functional + 21 scénarios installer + 5 scénarios opencode + 8 scénarios byok), `publishToMavenLocal` VERT, dogfooding réel `sh -n` + `docker compose config` + `jq` VALIDES, clé transmise au conteneur mais **absente du `opencode.json`**, sortie par défaut byte-identique ACADEMY-5, zéro secret.

'''

== ✅ EPIC ACADEMY-8 — Webhooks Moodle + bridge Ktor
_Source_ : Session 011 (**décision pilote 2026-09-21 : Ktor acté**). Transport
HTTP local opt-in : un producteur externe (Moodle, navigateur, `curl`) poste un
événement de formation, le bridge le traduit vers les contrats N0 de session et
l'acquitte. Le bridge **route et acquitte — il ne pense pas** : l'agent reste
opencode, zéro LLM dans le bridge.

_Dependances_ : ACADEMY-7 (BYOK) ✅, `runtime-contracts` + `opencode-session-contracts` (N0) ✅.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-8-0
| *Cadrage* — `CADRAGE_EPIC_ACADEMY_8_KTOR.adoc` (constats dont « le bridge codebase est stdin/stdout, pas HTTP » et « Moodle n'a pas de webhook natif », D-ACADEMY-8-1..10, options écartées).
| 3
| ✅ TERMINÉ (S-011)
| ACADEMY-8-1
| *Domaine pur `academy.bridge` + serveur Ktor* — `MoodleEvent` (schéma academy, payload opaque), `BridgeEventTranslator` (→ `SessionPrompt` N0, session id stable), `BridgeRoutes`, `BridgeServerConfig`, `BridgeWire` (anti-corruption — N0 non `@Serializable`), `ByokBridgeSupport`, `WebhookBridgeServer` (`installBridge` module unique, Netty).
| 8
| ✅ TERMINÉ (S-011)
| ACADEMY-8-2
| *Wiring + scaffold* — extension +3 properties (`bridgeEnabled` défaut `false`, `bridgeHost`, `bridgePort`), tâche `serveWebhookBridge` opt-in (`onlyIf`, seam `serverLauncher`), `InstallerPlatform` +3 champs, `AGENTS.md` documente le bridge si activé.
| 5
| ✅ TERMINÉ (S-011)
| ACADEMY-8-3
| *BDD + dogfooding* — `academy_bridge.feature` 6 scénarios `@bridge` + runner dédié + glue préfixée (S-088) ; dogfooding réel serveur + `curl` (`/health` 200, `POST /events/moodle` ack, 400/404, arrêt propre).
| 3
| ✅ TERMINÉ (S-011)
|===

_Total_ : 4 US, ~19 pts. _Methode_ : baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR).

_Statut_ : **EPIC ACADEMY-8 TERMINÉ (S-011)** — 8-0 ✅ + 8-1 ✅ + 8-2 ✅ + 8-3 ✅.

_Gates S-011_ : `clean check validatePlugins` VERT (311 unit + 10 functional + 21 installer + 5 opencode + 8 byok + 6 bridge scénarios), `publishToMavenLocal` VERT, dogfooding réel serveur + curl, tâche **SKIPPED** par défaut (prouvé TestKit), zéro secret.

'''

== ✅ EPIC ACADEMY-4 — Consommation du matériel versionné (MaterialUpdateContract)

_Source_ : Session 012. Academy **lit** le contrat N0 `MaterialUpdateContract`
comme déclaration d'exigence, vérifie le matériel présent (pivot EPIC K
`metadata.json`) et documente l'apprenant. Le **transport git appartient au
bureau** (`BUREAU-3`) — academy n'exécute jamais git (correction du BACKLOG,
D-ACADEMY-4-1).

_Dependances_ : ACADEMY-7/8 ✅, `runtime-contracts:0.0.1` (N0, Central) ✅.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-4-0
| *Cadrage* — `CADRAGE_EPIC_ACADEMY_4_MATERIAL.adoc` (9 constats, D-ACADEMY-4-1..12, options écartées). Corrige le BACKLOG : `MaterialUpdateContract` est une data class (pas une interface), le resolver appartient au bureau, `pullMaterial` n'existe pas encore.
| 3
| ✅ TERMINÉ (S-012)
| ACADEMY-4-1
| *Domaine pur `academy.material`* — `MaterialArtifactType` (6 livrables), `MaterialRequirement` (lecture N0), `MaterialManifest` (pivot EPIC K tolérant), `MaterialReadiness` (sealed), `MaterialInspector` (verdict pur), `MaterialGuideGenerator` ; 30 unit.
| 8
| ✅ TERMINÉ (S-012)
| ACADEMY-4-2
| *Wiring + scaffold* — extension +4 properties (`materialRemoteUrl`/`materialCurrentVersion`/`materialDir`/`materialExpectedTypes`), tâche `inspectTrainingMaterial` (n'échoue que si exigence déclarée), `InstallerPlatform.material`, scaffold `MATERIAL.md` conditionnel (bash + batch, source unique) ; +11 unit, +3 functional TestKit.
| 5
| ✅ TERMINÉ (S-012)
| ACADEMY-4-3
| *BDD + dogfooding* — `academy_material.feature` 8 scénarios `@material` + runner dédié + glue préfixée (S-088) ; dogfooding réel pivots EPIC K (READY/outdated/missing/Empty) + `sh -n` + `grep pullMaterial` = 0.
| 3
| ✅ TERMINÉ (S-012)
|===

_Total_ : 4 US, ~19 pts. _Methode_ : baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR).

_Statut_ : **EPIC ACADEMY-4 TERMINÉ (S-012)** — 4-0 ✅ + 4-1 ✅ + 4-2 ✅ + 4-3 ✅. US « alimenter le seed Moodle » (ancienne 4-2) **DIFFÉRÉE** (D-ACADEMY-4-12 : exige le contrat SPG/SPD canonique, MVP0 training non fait).

_Gates S-012_ : `clean check validatePlugins` VERT (152 unit + 13 functional + 21 installer + 5 opencode + 8 byok + 6 bridge + 8 material), `publishToMavenLocal` VERT, dogfooding réel, zéro secret, zéro `ProcessBuilder`/git dans le code prod.

'''

== ✅ Publication Maven Central — academy-plugin 0.0.1 (S-012)

_Demande pilote en cours de session._ Pré-requis vérifiés (version hardcodée,
POM Apache 2.0, signing `useGpgCmd`, dépendances releases sur Central, plugin
`com.gradleup.nmcp.settings` 1.5.0 ajouté à `academy-plugin/settings.gradle.kts`).

* `nmcpCheckAggregationFiles` VERT, puis `publishAggregationToCentralPortal`
  → **bundle `5c54af3f-1bfc-405f-950a-1172fff86b7a`** (AUTOMATIC).
* **Propagation confirmée** : `education.cccp:academy-plugin:0.0.1` **et**
  `education.cccp.academy.gradle.plugin:0.0.1` → **HTTP 200** (repo1).
* Consommation : `id("education.cccp.academy") version "0.0.1"` depuis Central.

'''

== 🟡 EPIC ACADEMY-10 — Migration du bridge Ktor → Spring Boot WebFlux (cadré S-012)

_Source_ : Session 012 (**décision pilote 2026-09-21**). Migrer le bridge
livré en ACADEMY-8 (Ktor 3.2.0 Netty) vers **Spring Boot WebFlux +
kotlinx-coroutines + R2DBC + PostgreSQL (Docker) + Jackson 3**.

_Faits vérifiés S-012_ : Spring Boot **4.1.1** GA (Maven Central) embarque
**Jackson 3.1.5** par défaut ; **Boot 3.4.2 (BOM workspace actuel) livre
Jackson 2.18.2** → incompatible avec l'exigence « jackson3 », donc **bump BOM
probable (cross-borough MEMPHIS)**. Jackson 3 = `tools.jackson.core` (groupId
changé). R2DBC : `spring-boot-starter-data-r2dbc` + `org.postgresql:r2dbc-postgresql`
(4.1.1 pin `1.1.2.RELEASE`) + `io.r2dbc:r2dbc-pool`.

_Référence de code vérifiée_ : `/home/cheroliv/sources/draft/api` (draft
personnel, tourne en Boot 3.5.9/Jackson 2 — **pattern source, pas source de
version**) : `Server` (`@SpringBootApplication`, 18 lignes), `CommandLine`
(`CommandLineRunner` + profil — répond à « tâche Gradle daemon vs `main()` »),
`DatabaseConfiguration` (R2DBC custom conversions `Instant ↔ LocalDateTime`,
`ConnectionFactoryInitializer` par profil), DDL co-localisé au domaine
(`UserActivation.Relations` : `SQL_SCRIPT`/`INSERT`/`UPDATE`), DAO
`DatabaseClient` + `awaitRowsUpdated()` en `suspend`, controller `suspend fun`
+ `ProblemDetail` (RFC 7807), `compose.yml` PostgreSQL.

_Dependances_ : ACADEMY-8 (bridge Ktor) ✅ — c'est une **migration**, pas une
création : le domaine pur `academy.bridge` (342 lignes, 7 fichiers) est
**conservé verbatim** (D-ACADEMY-8-4), seule la couche serveur migre.

_Statut_ : 🟡 CADRÉ (S-012) — section détaillée dans `BACKLOG.adoc`.
Cadrage formel dédié + découpage US à produire en session dédiée (pattern S-057).
**Non critique MVP0** — à ordonnancer sur décision pilote.

'''

== ✅ EPIC ACADEMY-11 — Injection du matériel pédagogique dans Moodle

_Source_ : Session 013 (**arbitrage pilote 2026-09-21**) puis S-016 (11-4). Academy **consomme**
la formation produite par training et **installe le runtime Docker** pour
l'acheteur ; il **injecte de la data structurée** AsciiDoc dans Moodle — il
**n'implémente pas** la boucle runtime. Cadrage :
`CADRAGE_EPIC_ACADEMY_11_MOODLE.adoc`.

_Découverte décisive_ : l'image `erseco/alpine-moodle:v5.2.3` **embarque des
outils natifs** (`moosh`, `moodle-blueprint`) — l'applicateur les **pilote** au
lieu de reconstruire une stack PHP/SQL.

_Dependances_ : ACADEMY-4 (matériel lu) ✅, ACADEMY-6 (runtime Docker) ✅.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-11-0
| *Cadrage* — `CADRAGE_EPIC_ACADEMY_11_MOODLE.adoc` (constats dont la frontière runtime et les outils natifs de l'image, D-ACADEMY-11-1..10, options écartées). Corrige `VISION_PIPELINE_FORMATION` § 6 et le TODO `SessionMemoryContract` périmé.
| 3
| ✅ TERMINÉ (S-013)
| ACADEMY-11-1
| *Domaine pur `academy.moodle`* — `MoodleActivityType` + `MoodleActivityMapping` (vocabulaire Moodle réel), `MoodleMaterial` (structure + pivot, path-only), `MoodleActivity`/`MoodleSection`, `MoodleImportPlan`, `MoodlePlanBuilder` (pur), `MoodleIngestionScriptGenerator` (plan JSON + applicateur) ; 20 unit.
| 8
| ✅ TERMINÉ (S-013)
| ACADEMY-11-2
| *Wiring* — extension +4 properties (opt-in), tâche `generateMoodleImport` (dégradée : matériel vide → rien écrit), scaffold `plan.json` + `ingest.sh` ; +7 unit + 11 unit opencode (ToolExposure).
| 5
| ✅ TERMINÉ (S-013)
| ACADEMY-11-3
| *BDD + dogfooding* — `academy_moodle.feature` 10 scénarios `@moodle` + runner dédié + glue préfixée (S-088) ; dogfooding réel Moodle 5.2.3 (cours/sections/activités injectés, 3 bugs réels attrapés).
| 3
| ✅ TERMINÉ (S-013)
| ACADEMY-11-4
| *Service compose one-shot `moodle-material`* — câbler le service dans `composeBody()` (applique `ingest.sh` après le schéma Moodle, pattern `moodle-seed`) + volume partagé `moodle-html` + garde de rejeu `sha256` + 2 scénarios BDD + dogfooding `compose up` réel.
| 3
| ✅ TERMINÉ (S-016)
|===

_Total_ : 5 US, ~22 pts. _Methode_ : baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR).

_Statut_ : ✅ **EPIC ACADEMY-11 TERMINÉ 4/4 US** — 11-0 ✅ S-013 + 11-1 ✅ S-013 + 11-2 ✅ S-013 + 11-3 ✅ S-013 + 11-4 ✅ S-016.

_Gates S-016_ : `clean check validatePlugins publishToMavenLocal` VERT **710 unit + 21 functional + 71 scénarios Cucumber** (24 installer + 6 opencode + 8 byok + 6 bridge + 8 material + 12 moodle + 7 env), dogfooding réel `compose up` (contenu en base, rejeu no-op), zéro secret, zéro SQL/DDL, zéro PHP maison, backward compat byte-identique sans opt-in.

'''

== 🟡 EPIC ACADEMY-12 — Provisionnement de l'engine conteneurs par OS

_Source_ : Session 014 (**arbitrage pilote 2026-09-25**). L'installeur suppose
Docker déjà présent ; ACADEMY-12 le **provisionne** par OS, avec Portainer
partout. Cadrage : `CADRAGE_EPIC_ACADEMY_12_WINDOWS.adoc`.

_Découverte décisive_ : Docker Engine natif Windows ne fait pas tourner les
images Linux, et « sans WSL » est impossible (Docker Desktop *gère* WSL2) →
**matrice d'engine actée** (Linux natif / Windows Desktop / macOS Colima).
Le runner GitHub Windows a été **écarté** (conteneurs Windows only).

_Dependances_ : ACADEMY-6 (installeur + compose) ✅.

[cols="1,3,1,1", options="header"]
|===
| US | Description | Pts | Statut
| ACADEMY-12-0
| *Cadrage* — `CADRAGE_EPIC_ACADEMY_12_WINDOWS.adoc` (constats, D-ACADEMY-12-1..8, options écartées) + **préalable P0** `settings.gradle.kts` tolérant à l'absence de creds (les 25 boroughs partageaient le blocage).
| 3
| ✅ TERMINÉ (S-014)
| ACADEMY-12-1
| *Domaine pur `academy.env`* — `HostOs`, `EnvironmentFacts`, `DockerStrategy` (sealed : AlreadyReady/InstallDockerEngine/InstallColima/InstallDockerDesktop/BootstrapWsl2First/Unsupported), `DockerStrategyDecider` (pur) ; 10 unit + BDD `academy_env.feature` 7 scénarios + runner `cucumberTestEnv`.
| 5
| ✅ TERMINÉ (S-014)
| ACADEMY-12-2
| *Writers + sonde adapter* — `install.sh` portable (`unixScript` : en-tête Linux apt officiel / macOS Colima, **queue commune verbatim**) + `install.bat` (sonde admin+WSL2, WSL2/Desktop resumable) + port `EnvironmentProbe` + adapter `HostEnvironmentProbe` + `HostOs.fromOsName` + task `checkContainerEngine` (verdict rapporté) + functional TestKit + dogfooding Linux (`bash -n`, `docker compose config`, idempotence).
| 5
| ✅ TERMINÉ (S-015)
| ACADEMY-12-3
| *Dogfooding environnement Windows réel* — `install.bat`, `docker compose config`, parité, images tirées ; **jamais livré sans preuve**.
| 3
| 🟡 À FAIRE
|===

_Total_ : 4 US, ~16 pts. _Methode_ : baby-step DDD/TDD/BDD strict (RED → GREEN → REFACTOR).

_Statut_ : 🟡 EN COURS 3/4 US — 12-0 ✅ S-014 + 12-1 ✅ S-014 + 12-2 ✅ S-015 ; **12-3 à faire**.

_Gates S-015_ : `clean check validatePlugins` VERT **207 unit + 16 functional + 69 scénarios Cucumber** (24 installer + 6 opencode + 8 byok + 6 bridge + 8 material + 10 moodle + 7 env), `publishToMavenLocal` VERT, `help -Duser.home=<vide>` VERT (P0), dogfooding Linux réel (`bash -n` OK, `docker compose config` exit 0, idempotence), zéro secret, zéro `ProcessBuilder` dans le domaine.

'''

== ✅ EPIC ACADEMY-2/3 — CLOS côté academy (frontière runtime actée S-013)

_Source_ : Sessions 009 (D-ACADEMY-5-8) → 013 (**arbitrage pilote**).

*Décision (D-ACADEMY-11-1)* : la boucle runtime (`FormationRuntimePort`,
`InteractionProtocol`) **n'est pas implémentée par academy**. Trois sources
convergent (`VISION_FORMATION_INTERACTIVE` § D3 + itération 9, KDoc
`BureauPlugin.kt:24`, règle licence 3 étages) : l'orchestration agentique est
l'étage **privé** (workspace-gradle/edster, LocalAdapter + CloudAdapter).
Academy **consomme** la formation et **injecte la data** dans Moodle.

_Découverte S-013_ : le TODO « `SessionMemoryContract` zéro implémenteur » est
**périmé** — `codex-plugin/.../codex/profile/PgVectorSessionMemory.kt:37`
l'implémente depuis S-094 (CDX-RC-04, pgvector + ONNX).

_Statut_ : ✅ CLOS côté academy (la boucle est un EPIC privé workspace-gradle).
`ToolExposure` (N0), lui, est **branché** (S-013 — `OpenCodeCatalog.exposure()`).

'''

== Sessions

|===
| # | Date | Type | Sujet | Score
| 000 | 2026-09-19 | bootstrap | Initialisation du borough academy-gradle | 2/2
| 001 | 2026-09-19 | architecture | Alignement article 0124 + fusion gouvernance + licence Apache 2.0 + repo Git | 6/6
| 002 | 2026-09-20 | refonte | Recadrage de l'orientation — installeur hybride (vision actée, D-ACADEMY-1..4) | 3/3
| 003 | 2026-09-20 | développement | Socle générateur d'installateurs (ACADEMY-1 3/3 US, BDD 5 scénarios) + cadrage ACADEMY-6-0 | 17/17
| 004 | 2026-09-22 | développement | Révisions pilote du scaffold compose (Ollama + Portainer + PostgreSQL, parité Linux/Windows) | 5/5
| 005 | 2026-09-22 | développement | ACADEMY-6-4 TERMINÉ — compose complet (network, volumes nommés, healthcheck PostgreSQL, `.env`) + parité structurelle Linux/Windows + dogfooding `docker compose config` | 5/5
| 006 | 2026-09-22 | développement | ACADEMY-6-5 TERMINÉ — seed DB data-only (service one-shot `moodle-seed`, entrypoint idempotent, SQL `WHERE NOT EXISTS`) + parité structurelle Linux/Windows + idempotence prouvée PostgreSQL 16 réel | 5/5
| 007 | 2026-09-21 | développement | ACADEMY-6-6 TERMINÉ — dogfooding bout-en-bout : 5 écarts découverts + corrigés (Moodle réel 5.2.3, contrat env `DB_*`/`MOODLE_*`, portainer-ce, workspace différé) ; run réel complet prouvé | 5/5
| 008 | 2026-09-21 | développement | ACADEMY-6-7 TERMINÉ + EPIC ACADEMY-6 TERMINÉ — Cucumber BDD étendu (contenu OS, chemins, idempotence, erreurs, opencode.json différé) + hôte = Docker uniquement (D-ACADEMY-6-10 appliqué) ; 6-1/2/3 CADUQUES absorbés | 5/5
| 009 | 2026-09-21 | développement | ACADEMY-5 TERMINÉ — exposition agent opencode : domaine `academy.opencode` (6 fichiers, 11 plugins catalogués), service `workspace` (profile agent, TODO S-007 résolu), `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` générés (parité Linux/Windows), extension 3 properties ; dogfooding réel (build image, `opencode 1.18.31` learner, bug install-root D-ACADEMY-5-11) | 11/11
| 010 | 2026-09-21 | développement | ACADEMY-7 TERMINÉ — BYOK LLM multi-provider : domaine `academy.byok` (2 fichiers, mapping `LlmProviderKind` → provider opencode réel vérifié), `OpenCodeConfig`/`OpenCodeConfigGenerator`/`LearnerGuideGenerator` multi-provider (`{env:NAME}`, jamais la valeur), extension +2 properties, `.env` nom-seul + compose transmet, dépendance N0 `runtime-contracts` ; dogfooding réel GEMINI + défaut (clé absente du JSON, byte-identité ACADEMY-5) | 14/14
| 011 | 2026-09-21 | développement | ACADEMY-8 TERMINÉ — webhooks Moodle + bridge Ktor (décision pilote) : domaine `academy.bridge` (schéma academy, traduction N0 à session stable, routes, anti-corruption `BridgeWire`), serveur Ktor 3.2.0 Netty, tâche `serveWebhookBridge` opt-in (SKIPPED par défaut), extension +3 properties, `AGENTS.md` conditionnel ; dogfooding réel serveur + curl (400/404) ; correction de gouvernance (bridge codebase = stdin/stdout ≠ HTTP) | 19/19
| 012 | 2026-09-21 | développement + publication | ACADEMY-4 TERMINÉ — consommation du matériel versionné : domaine `academy.material` (lecture N0 + pivot EPIC K, verdict pur sealed), tâche `inspectTrainingMaterial` (n'échoue que si exigence déclarée), extension +4 properties, scaffold `MATERIAL.md` conditionnel ; frontière des verbes corrigée (transport git = bureau, academy lit — zéro git) ; dogfooding réel pivots EPIC K ; **publication Maven Central 0.0.1** (bundle `5c54af3f`, propagation HTTP 200) ; EPIC ACADEMY-10 cadré (Spring Boot WebFlux) | 19/19
| 013 | 2026-09-21 | développement + cadrage | ACADEMY-11 3/4 US + ToolExposure (N0) + frontière runtime actée — injection du matériel dans Moodle : domaine `academy.moodle` (vocabulaire Moodle réel, plan pur, applicateur), tâche `generateMoodleImport` (opt-in, dégradée), extension +4 properties ; **outils natifs de l'image découverts** (`moosh`, `moodle-blueprint`) — l'applicateur les pilote (zéro PHP maison, zéro SQL/DDL) ; dogfooding réel Moodle 5.2.3 + PostgreSQL 16 (3 bugs réels attrapés) ; frontière actée (academy ne porte pas `FormationRuntimePort`, D-ACADEMY-11-1) ; `OpenCodeCatalog.exposure()` (ToolExposure N0) | 19/19 |
| 014 | 2026-09-25 | cadrage + développement | **ACADEMY-12 cadré + ACADEMY-12-1 TERMINÉ** — provisionnement engine conteneurs par OS : matrice actée (Linux natif / Windows Docker Desktop WSL2 / macOS Colima), Portainer partout, supersede D-ACADEMY-6-12 ; domaine pur `academy.env` (`DockerStrategyDecider` sealed) + BDD 7 scénarios `@env` ; préalable P0 `settings.gradle.kts` tolérant (hôte nu configure) ; runner GitHub Windows écarté (faits) | 8/8 |
| 015 | 2026-09-25 | développement | **ACADEMY-12-2 TERMINÉ** — writers portables + sonde adapter + dogfooding Linux : l'installeur **provisionne** (un `install.sh` unique Linux apt / macOS Colima, queue commune verbatim ; `install.bat` sonde admin+WSL2 en faits puis WSL2/Desktop resumable) ; port `EnvironmentProbe` + adapter `HostEnvironmentProbe` + `HostOs.fromOsName` + task `checkContainerEngine` (verdict rapporté, jamais un throw) ; dogfooding Linux réel (`bash -n` OK, `docker compose config` exit 0, idempotence marquée) ; 207 unit + 16 functional + 69 Cucumber 0 FAIL | 8/8 |
| 016 | 2026-09-25 | développement | **ACADEMY-11-4 TERMINÉ + EPIC ACADEMY-11 CLOS** — service compose one-shot `moodle-material` : volume nommé `moodle-html` partagé (`moosh` bootstrappe contre l'install), `depends_on: moodle: service_healthy`, garde de rejeu `sha256` (`.academy-applied-plan` dans le volume inscriptible, car `moosh activity-add` ne dédoublonne pas), `GenerateInstallerTask.moodleMaterialFiles` input tolérant (fin de la staleness), `MoodleMaterialReader` partagé ; dogfooding réel `compose up` (cours/sections/activités + contenu AsciiDoc en base, rejeu no-op prouvé, **3 bugs réels attrapés**) ; 710 unit + 21 functional + 71 Cucumber 0 FAIL | 8/8 |
|===

'''
== Portefeuille — Projets Gouvernes

|===
| Projet | Chemin | Sessions | Derniere maj
| planner-gradle | `foundry/public/planner-gradle/` | 17 | 2026-06-24
| codex-gradle | `foundry/public/codex-gradle/` | 26 | 2026-05-22
| codebase-gradle | `foundry/public/codebase-gradle/codebase-plugin/` | 55 | 2026-05-22
| quizz-benchmark | `foundry/public/quizz-benchmark-gradle/quizz-benchmark-plugin/` | 6 | 2026-05-11
| runner-gradle | `foundry/public/runner-gradle/` | 17 | 2026-05-19
| waiter-gradle | `foundry/public/waiter-gradle/` | 0 (bootstrap) | 2026-05-11
| training-gradle | `foundry/public/training-gradle/training-plugin/` | 10 | 2026-05-14
| capsule-gradle | `foundry/public/capsule-gradle/capsule-plugin/` | 11 | 2026-05-19
| workspace-gradle | `foundry/public/workspace-gradle/` | 1 | 2026-05-17
| magic-stick | `office/sites/magic-stick/` | 4 | 2026-05-11
| academy-gradle | `foundry/public/academy-gradle/` | 17 | 2026-09-25
|===

'''