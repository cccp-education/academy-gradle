= COMPLETED_TASKS_ARCHIVE — 2026-09 — academy-gradle

_Session 000 (2026-09-19) — Bootstrap_

- ACADEMY-0-0 : Bootstrap academy-gradle — gouvernance, structure plugin, placeholder (2 pts)

_Session 001 (2026-09-19) — Alignement architecture 0124 + gouvernance + repo Git_

- ACADEMY-0-BIS-0 : Fusion gouvernance racine → academy-plugin/.agents (2 pts)
- ACADEMY-0-BIS-1 : Alignement article 0124 (wrappers, catalogues, builds racine/plugin, signing, .gitignore) (3 pts)
- ACADEMY-0-BIS-2 : Licence Apache 2.0 + vérification repo GitHub cccp-education (1 pt)

_Session 002 (2026-09-20) — Recadrage de l'orientation — installeur hybride_

- ACADEMY-0-TER-0 : Recadrage BACKLOG — vision hybride actée, D-ACADEMY-1..4, ACADEMY-1 pivoté, ACADEMY-6 révisé (2 pts)
- ACADEMY-0-TER-1 : Suppression fichiers hors-projet files/ + files.zip (com.example.installer) (1 pt)

_Session 003 (2026-09-20) — Socle générateur d'installateurs (ACADEMY-1) + cadrage ACADEMY-6-0_

- ACADEMY-1-0 : Extension `academyInstaller` Property-based (8 properties) + plugin wiring (3 pts)
- ACADEMY-1-1 : Domaine pur `education.cccp.academy.installer` (TargetOs, InstallerPlatform+invariants, InstallerFile, InstallerScriptGenerator) (3 pts)
- ACADEMY-1-2 : Tests — 10 unit + 2 functional TestKit + 5 scénarios cucumber (3 pts)
- ACADEMY-6-0 : Cadrage `CADRAGE_EPIC_ACADEMY_INSTALLER.adoc` (5 constats, 9 décisions, articulation 1→6→5→7) (8 pts)

_Session 004 (2026-09-22) — Révisions pilote du scaffold compose (Ollama, Portainer, PostgreSQL)_

- ACADEMY-6-4 (volet compose scaffold) : gouvernance actée (D-6-10→13) + swap `mariadb:11`→`postgres:16` sur Linux/macOS et Windows + parité Windows `ollama`/`portainer` — RED prouvé (3 failed) → GREEN (12/12, check VERT) (5 pts)

_Session 005 (2026-09-22) — ACADEMY-6-4 TERMINÉ : compose complet + parité structurelle Linux/Windows_

- ACADEMY-6-4 : `docker-compose.yml` complet et exécutable — network dédié `academy-net`, volumes nommés `postgres-data`/`ollama-models`/`portainer-data`, healthcheck PostgreSQL `pg_isready` + `depends_on: service_healthy`, Moodle câblé `pgsql`, `.env` généré (noms seulement). REFACTOR `composeBody()`/`envBody()` = source unique partagée bash/batch (parité structurelle). Tests : 2 unit + 1 functional TestKit + 2 scénarios BDD. Dogfooding `docker compose config` VALIDE. Gates : 43 checks 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT (5 pts)

_Session 006 (2026-09-22) — ACADEMY-6-5 TERMINÉ : seed DB data-only au premier `compose up`_

- ACADEMY-6-5 : service one-shot `moodle-seed` (image `postgres:16`, `restart: "no"`, `entrypoint sh /seed/seed.sh`, `./seed:/seed`, `depends_on postgres healthy`) dans `composeBody()` ; corps seed **sources uniques** `seedScriptBody()` (poll `mdl_course` puis `psql -f seed-course.sql`) + `seedCourseSqlBody()` (`INSERT ... WHERE NOT EXISTS` sur `idnumber='academy-seed'`, zéro DDL/DELETE/UPDATE) ; writers bash heredoc + batch `> ... (` avec `escapeCmd()` (caret-escape `^ > & ( )`). Tests : 4 unit + 1 functional TestKit + 4 scénarios BDD. Dogfooding : `docker compose config` VALIDE, `sh -n seed.sh` VALIDE, **idempotence prouvée sur PostgreSQL 16 réel** (run1 `INSERT 0 1`, run2 `INSERT 0 0`, count=1). Gates : 16 unit + 4 functional + 11 scénarios Cucumber ×2 runners, 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT (5 pts)

_Session 007 (2026-09-21) — ACADEMY-6-6 TERMINÉ : dogfooding bout-en-bout + correction des écarts_

- ACADEMY-6-6 : dogfooding réel (build installeur Linux, extraction heredocs, `docker compose up` éphémère) — **prouve l'enchaînement** (PostgreSQL/Ollama healthy, seed `INSERT 0 1`, Moodle 5.2.3 `/login` HTTP 200, idempotence `INSERT 0 0`) **et révèle 5 écarts de scaffold inexécutable**. Gouvernance (pattern S-057) : D-ACADEMY-6-14 (image `erseco/alpine-moodle:v5.2.3`, suivre le latest), D-ACADEMY-6-15 (contrat env réel `DB_*`/`MOODLE_*`), D-ACADEMY-6-16 (portainer-ce + workspace différé TODO ACADEMY-5), D-ACADEMY-6-17 (pas de healthcheck Portainer — distroless). RED 20 failed → GREEN ; tests : 5 unit + 4 scénarios BDD + 1 functional TestKit. Gates : 21 unit + 5 functional + 15 scénarios Cucumber ×2 runners, 0 FAIL, validatePlugins VERT, publishToMavenLocal VERT (5 pts)

_Session 008 (2026-09-21) — ACADEMY-6-7 TERMINÉ + EPIC ACADEMY-6 TERMINÉ : Cucumber BDD étendu + hôte = Docker uniquement_

- ACADEMY-6-7 : Cucumber BDD étendu (contenu OS, chemins, idempotence, erreurs, `opencode.json` différé) + **alignement du contenu OS sur D-ACADEMY-6-10** (hôte = Docker uniquement, zéro provisioning Java/Gradle hôte — D-ACADEMY-6-18). RED prouvé aux 3 niveaux (unit 6 failed / cucumber 6 failed / functional 1 failed) → GREEN. Tests : +7 unit (25 total), +6 scénarios BDD (21 total), +1 TestKit (6 total). Gates : `clean check validatePlugins` VERT, publishToMavenLocal VERT, dogfooding `sh -n` + `docker compose config` VALIDES, zéro secret, zéro `mariadb` (5 pts)
- ACADEMY-6-1/2/3 : **✅ CADUQUES (S-008)** — raffermissement hôte Java/Gradle sans objet après application de D-ACADEMY-6-10 (absorbés par ACADEMY-6-7)

_Session 009 (2026-09-21) — ACADEMY-5 TERMINÉ : exposition agent opencode_

- ACADEMY-5-0 : Cadrage `CADRAGE_EPIC_ACADEMY_5_OPENCODE.adoc` (constats, D-ACADEMY-5-1..11, options écartées, articulation 5→7→8→2/3). Découverte : l'image officielle `ghcr.io/anomalyco/opencode` est Alpine-minimal (inapte Gradle) → Dockerfile généré sur `gradle:9.7.1-jdk25` (3 pts)
- ACADEMY-5-1 : Domaine pur `education.cccp.academy.opencode` (6 fichiers — `OpenCodeConfig`/`OpenCodePlugin`/`OpenCodeCatalog` 11 plugins publics/`OpenCodeConfigGenerator`/`LearnerGuideGenerator`/`WorkspaceDockerfileGenerator`) + service `workspace` (profile `agent`, TODO S-007 résolu) + `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` générés (parité Linux/Windows) + extension 3 properties + runner `cucumberTestOpenCode` ; dogfooding réel (build image, `opencode 1.18.31` learner, bug install-root D-ACADEMY-5-11 corrigé). Tests : +22 unit (50 total), +5 scénarios opencode, 2 scénarios installer migrés, +1 functional (7 total) (8 pts)
- ACADEMY-5-2 : **✅ ABSORBÉE (S-009)** par ACADEMY-5-1 — le `project/` généré EST le projet consommateur (Dockerfile + opencode.json + AGENTS.md + README du premier `gradle build`)

_Session 010 (2026-09-21) — ACADEMY-7 TERMINÉ : BYOK LLM multi-provider_

- ACADEMY-7-0 : Cadrage `CADRAGE_EPIC_ACADEMY_7_BYOK.adoc` (constats, D-ACADEMY-7-1..10, mapping provider vérifié models.dev/docs, options écartées, articulation 5→7→8→2/3). Supersede D-ACADEMY-5-9 (dépendance N0 assumée) (3 pts)
- ACADEMY-7-1 : Domaine pur `education.cccp.academy.byok` (`ByokProviderSpec` + `ByokProviderCatalog`, mapping total `LlmProviderKind` → provider opencode réel) + `OpenCodeConfig` +2 champs + `OpenCodeConfigGenerator` multi-provider (`apiKey` toujours `{env:NAME}`) + `LearnerGuideGenerator` BYOK concret + dépendance N0 `runtime-contracts`. Tests : +21 unit (dont **test de byte-identité ACADEMY-5**), BDD byok (5 pts)
- ACADEMY-7-2 : Extension +2 properties (`openCodeProvider` défaut `OLLAMA_LOCAL`, `openCodeApiKeyEnvVar`) + `InstallerPlatform`/`GenerateInstallerTask` + `.env` nom-seul + `composeBody()` transmet la variable au service `workspace` + parité Linux/Windows. Tests : +7 unit + 2 TestKit (3 pts)
- ACADEMY-7-3 : BDD `academy_byok.feature` `@byok` 8 scénarios + runner dédié `cucumberTestByok` (S-082) + glue préfixée `AcademyByokSteps` (S-088 anti-collision) ; dogfooding réel GEMINI + défaut (clé transmise au compose, **absente du `opencode.json`**, `sh -n`/`compose config`/`jq` VALIDES, byte-identité ACADEMY-5) (3 pts)

_Session 011 (2026-09-21) — ACADEMY-8 TERMINÉ : webhooks Moodle + bridge Ktor_

- ACADEMY-8-0 : Cadrage `CADRAGE_EPIC_ACADEMY_8_KTOR.adoc` (D-ACADEMY-8-1..10, options écartées, articulation 7→8→4→2/3). Constats vérifiés : « le bridge codebase est stdin/stdout, pas HTTP » (justification D4 inexacte), « Moodle n'a pas de webhook natif » → schéma academy (3 pts)
- ACADEMY-8-1 : Domaine pur `education.cccp.academy.bridge` (`MoodleEvent` payload opaque, `BridgeEventTranslator` → `SessionPrompt` N0 à session id **stable**, `BridgeRoutes`, `BridgeServerConfig`, `BridgeWire` anti-corruption — N0 non `@Serializable`, `ByokBridgeSupport`) + serveur `WebhookBridgeServer` (Ktor 3.2.0 Netty, `installBridge` module unique test+prod, acks, 400/404). Tests : +23 unit dont 7 `testApplication` HTTP sans port réel ; BDD 6 scénarios (8 pts)
- ACADEMY-8-2 : Extension +3 properties (`bridgeEnabled` défaut `false`, `bridgeHost` `127.0.0.1`, `bridgePort` `8765`) + tâche `serveWebhookBridge` **opt-in** (`onlyIf`, seam `serverLauncher`) + `InstallerPlatform` +3 champs + `AGENTS.md` section bridge conditionnelle (`BridgeGuide`). Tests : +4 unit + 1 TestKit « tâche SKIPPED » (5 pts)
- ACADEMY-8-3 : BDD `academy_bridge.feature` `@bridge` 6 scénarios + runner dédié `cucumberTestBridge` (S-082) + glue préfixée `AcademyBridgeSteps` (S-088) ; dogfooding réel serveur + `curl` (`/health` 200 provider+model, `POST /events/moodle` ack session stable, `POST /session` ack, JSON invalide 400, route inconnue 404, arrêt propre) (3 pts)
_Session 012 (2026-09-21) — ACADEMY-4 TERMINÉ : consommation du matériel versionné + publication Maven Central_

- ACADEMY-4-0 : Cadrage `CADRAGE_EPIC_ACADEMY_4_MATERIAL.adoc` (9 constats, D-ACADEMY-4-1..12, options écartées, articulation 8→4→2/3). **Corrige le BACKLOG** : `MaterialUpdateContract` est une data class (le resolver est `MaterialUpdateResolver`), le transport git appartient au **bureau** (`BUREAU-3`), `pullMaterial` n'existe pas encore (mention KDoc seulement — jamais cité comme exécutable, leçons S-007/S-011) (3 pts)
- ACADEMY-4-1 : Domaine pur `education.cccp.academy.material` (6 fichiers — `MaterialArtifactType` 6 livrables + `fromPivotType` qui ne devine jamais, `MaterialRequirement` = lecture du N0, `MaterialManifest` = lecture du **pivot EPIC K** `metadata.json` tolérante (`ignoreUnknownKeys`, JSON malformé → null), `MaterialReadiness` sealed `Ready`/`Incomplete`/`Empty`, `MaterialInspector` verdict pur sans exception, `MaterialGuideGenerator`). Tests : +30 unit (8 pts)
- ACADEMY-4-2 : Extension +4 properties (`materialRemoteUrl` défaut vide, `materialCurrentVersion`, `materialDir` défaut `material`, `materialExpectedTypes`) + tâche `InspectTrainingMaterialTask` (n'échoue **que** si exigence déclarée — `Empty` succès, la ressource est générée avant le pull) + `InstallerPlatform.material` + `GenerateInstallerTask` + scaffold `MATERIAL.md` **conditionnel** (source unique bash/batch, parité structurelle). Tests : +11 unit + 3 functional TestKit (5 pts)
- ACADEMY-4-3 : BDD `academy_material.feature` `@material` 8 scénarios + runner dédié `cucumberTestMaterial` (S-082) + glue préfixée `AcademyMaterialSteps` (S-088) ; dogfooding réel consommateur /tmp + pivots EPIC K réels → READY / outdated / missing / Empty, `MATERIAL.md` généré + `sh -n` VALIDE + `grep pullMaterial` = 0. **Découverte dogfooding** : exigence par types seuls silencieusement ignorée → correctif TDD `hasRequirement()` (3 pts)
- Publication Maven Central : plugin `com.gradleup.nmcp.settings` 1.5.0 (settings), `nmcpCheckAggregationFiles` VERT, bundle **`5c54af3f-1bfc-405f-950a-1172fff86b7a`** AUTOMATIC, **propagation HTTP 200** (academy-plugin + marker `education.cccp.academy.gradle.plugin`) — demande pilote en cours de session
- ACADEMY-10 : **CADRÉ** (migration du bridge Ktor → Spring Boot WebFlux + kotlinx-coroutines + R2DBC + PostgreSQL Docker + Jackson 3) — cadrage formel et découpage US à produire en session dédiée. Référence de code `sources/draft/api` analysée
- ACADEMY-4-2 (ancienne, « alimenter le seed Moodle ») : **DIFFÉRÉE** (D-ACADEMY-4-12 — exige le contrat SPG/SPD canonique, MVP0 training non fait)
