= SESSIONS_HISTORY — academy-gradle

|===
| # | Date | Sujet | Points | Résumé
| 000 | 2026-09-19 | Bootstrap academy-gradle (Bagdad) | 2 | Initialisation du borough : gouvernance (AGENT, SESSION_CHECKLIST, INDEX, PROMPT_REPRISE), structure plugin academy-plugin, placeholder AcademyPlugin.kt, compilation + publishToMavenLocal.
| 001 | 2026-09-19 | Alignement architecture 0124 + gouvernance + repo Git | 6 | Fusion gouvernance racine → academy-plugin/.agents (git mv 100% rename), .gitignore + untrack artefacts, wrappers 9.7.1, catalogues séparés, build racine consommateur (zéro include), build plugin autonome (toolchain 25, functionalTest, cucumberTest, signing, POM Apache 2.0), licence Apache 2.0, repo cccp-education vérifié. publishToMavenLocal BUILD SUCCESSFUL. Push différé.
| 002 | 2026-09-20 | Recadrage de l'orientation — installeur hybride | 3 | Arbitrage pilote : la ressource academy = machine d'expérimentation complète (hôte Docker+Java25+Gradle + Docker Moodle+MariaDB+workspace opencode zsh). Suppression files/ + files.zip (com.example.installer rejeté). BACKLOG recadré (ACADEMY-1 socle générateur, ACADEMY-6 installeur hybride US-0 ✅, D-ACADEMY-1..4). INDEX + PROMPT_REPRISE mis à jour.
| 003 | 2026-09-20 | Socle générateur d'installateurs (ACADEMY-1) + cadrage ACADEMY-6-0 | 17 | ACADEMY-1 TERMINÉ 3/3 US : extension `academyInstaller` Property-based (8 properties), domaine pur `education.cccp.academy.installer` (TargetOs, InstallerPlatform+invariants, InstallerFile, InstallerScriptGenerator), tâches generateLinux/Windows/MacInstaller + buildAllInstallers (thin-wrapper GenerateInstallerTask, @DisableCachingByDefault). Tests : 10 unit + 2 functional TestKit + Cucumber `academy_installer.feature` @installer 5 scénarios (runner S-082 `AcademyInstallerCucumberRunner`, TaskSpec `cucumberTestInstaller` branché à check). Gates: check VERT, validatePlugins VERT, publishToMavenLocal VERT. ACADEMY-6-0 : `CADRAGE_EPIC_ACADEMY_INSTALLER.adoc` (5 constats, 9 décisions, US 6-1..7, options écartées, articulation 1→6→5→7). Commit + push origin cccp-education/academy-gradle.
| 004 | 2026-09-22 | Révisions pilote du scaffold compose (Ollama, Portainer, PostgreSQL) | 5 | ACADEMY-6-4 (volet compose) avancé : baby-step TDD strict RED → GREEN sur le scaffold compose du `InstallerScriptGenerator`. Gouvernance d'abord (pattern S-057) : CADRAGE `D-ACADEMY-6-5` aligné, doublon `D-ACADEMY-6-10` consolidé en 4 décisions (6-10 hôte=Docker only, 6-11 Ollama, 6-12 Portainer remplace Docker Desktop, 6-13 PostgreSQL remplace MariaDB) ; BACKLOG (schéma, table épics, orientation, ACADEMY-6-4, D-ACADEMY-1, historique). RED prouvé (`postgres`+`POSTGRES_PASSWORD`+`assertFalse(mariadb)` sur Linux et Windows + feature Cucumber) → **12 tests, 3 failed**. GREEN : scaffold Linux/macOS `mariadb:11`→`postgres:16`, scaffold Windows même swap **+ ajout ollama + portainer** (parité cross-plateforme cadenassée). KDoc alignés (3 fichiers). Gates : `check` VERT, `publishToMavenLocal` VERT, zéro `mariadb` en prod. |
| 005 | 2026-09-22 | ACADEMY-6-4 TERMINÉ — compose complet + parité structurelle Linux/Windows | 5 | Suite de S-004 (ordre 1 → 6 → 5 → 7), baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR. **RED** : 2 unit (network/volumes nommés/healthcheck PostgreSQL/`.env` + parité Windows) + 2 scénarios Cucumber → `test` 14 tests **2 failed**, `cucumberTestInstaller` **2 scénarios failed**. **GREEN + REFACTOR** : extraction de `composeBody()`/`envBody()` = **source unique partagée** par le heredoc bash (`appendComposeScaffold`) et le writer batch (`appendWindowsComposeScaffold`) — la parité n'est plus assertée a posteriori, elle est structurellement garantie (la classe de bug de drift par duplication S-004 est supprimée). Network dédié `academy-net` (bridge), volumes nommés `postgres-data`/`ollama-models`/`portainer-data`, healthcheck PostgreSQL `pg_isready` + `depends_on: service_healthy`, Moodle câblé `pgsql`, `.env` généré (noms seulement, `POSTGRES_PASSWORD=` vide — plus de `changeme` hardcodé). Tests ajoutés : 2 unit + 1 functional TestKit (écriture réelle des fichiers sur disque, shell + batch) + 2 scénarios BDD. **Dogfooding réel** : `gradle buildAllInstallers` sur le consommateur racine puis `docker compose config --quiet` sur le YAML extrait → **COMPOSE VALIDE** (directement exécutable). Gates : `clean check` VERT **43 checks** (26 unit + 14 cucumber + 3 functional) 0 FAIL, `validatePlugins` VERT, `publishToMavenLocal` VERT, zéro secret hardcodé, zéro `mariadb` en prod. |
| 006 | 2026-09-22 | ACADEMY-6-5 TERMINÉ — seed DB data-only au premier `compose up` | 5 | Suite de S-005 (ordre 1 → 6 → 5 → 7), baby-step DDD/TDD/BDD strict RED → GREEN → REFACTOR, gouvernance d'abord (pattern S-057 : `D-ACADEMY-6-6` précisée = service one-shot `moodle-seed`, SQL data-only idempotent, staging `$APP_DIR/seed/`). **RED prouvé par stash de la prod** : 4 unit + 4 scénarios Cucumber (×2 runners = 8 failed) + 1 functional TestKit → `test` **20 tests 4 failed**, `cucumberTestInstaller` **4 scénarios failed**, `functionalTest` **1 failed**. **GREEN** : service `moodle-seed` (image `postgres:16` réutilisée, `restart: "no"`, `entrypoint ["sh","/seed/seed.sh"]`, env `PG*` depuis `${...}`, `./seed:/seed`, `depends_on postgres healthy`) ajouté à `composeBody()` ; `seedScriptBody()` (`set -eu`, poll `until psql ... to_regclass('public.mdl_course')` max 60 tentatives, puis `psql -v ON_ERROR_STOP=1 -f /seed/seed-course.sql`) ; `seedCourseSqlBody()` = **un seul** `INSERT INTO mdl_course (...) SELECT ... WHERE NOT EXISTS (SELECT 1 ... idnumber='academy-seed')` (zéro DDL/DELETE/UPDATE/TRUNCATE). **REFACTOR** : corps seed **sources uniques** rendus par le heredoc bash et le writer batch `> ... (` avec `escapeCmd()` (caret-escape `^ > & ( )` pour `2>&1` et `to_regclass(...)`) — parité structurelle garantie par construction (leçon S-005). **Dogfooding réel** : `publishToMavenLocal` + `buildAllInstallers` sur le consommateur racine, extraction des heredocs → `docker compose config --quiet` **VALIDE**, `sh -n seed.sh` **VALIDE**, et **idempotence SQL prouvée sur PostgreSQL 16 réel** (conteneur jetable + table `mdl_course` synthétique : run 1 `INSERT 0 1`, run 2 `INSERT 0 0`, `count(*) = 1`). Gates : `clean check validatePlugins` VERT **16 unit + 4 functional + 11 scénarios Cucumber ×2 runners**, `publishToMavenLocal` VERT, zéro secret, zéro `mariadb`. |
| 007 | 2026-09-21 | ACADEMY-6-6 TERMINÉ — dogfooding bout-en-bout + correction des écarts | 5 | Suite de S-006 (ordre 1 → 6 → 5 → 7). **Le dogfooding a payé** : build réel de l'installeur Linux, extraction des heredocs, `docker compose up` sur conteneur éphémère → il a **prouvé l'enchaînement** (PostgreSQL/Ollama healthy, seed one-shot `INSERT 0 1`, Moodle 5.2.3 `/login/index.php` HTTP 200) **et révélé que le scaffold livré était inexécutable**. **5 écarts découverts** : (1) `moodle:4.5` **n'existe pas** (`pull access denied`, placeholder jamais vérifié) ; (2) contrat env faux `MOODLE_DATABASE_*` (aucune image réelle ne l'expose — contrat effectif `DB_TYPE/DB_HOST/DB_NAME/DB_USER/DB_PASS` + `MOODLE_USERNAME/PASSWORD/SITENAME`) ; (3) version périmée 4.5 vs dernière **5.2.3** ; (4) `portainer/portainer` obsolète vs `portainer/portainer-ce` ; (5) healthcheck Portainer `wget` invalide (image **distroless**, ni `sh` ni `wget` → `unhealthy` permanent alors que HTTP 200) + service `workspace` à l'image non publiée qui aurait cassé `compose up`. **Gouvernance d'abord (pattern S-057)** : `D-ACADEMY-6-14` (image `erseco/alpine-moodle:v5.2.3`, suivre le latest — décisions pilote), `D-ACADEMY-6-15` (contrat env réel), `D-ACADEMY-6-16` (portainer-ce + workspace différé, TODO ACADEMY-5), `D-ACADEMY-6-17` (pas de healthcheck Portainer). **RED prouvé** (prod au HEAD) : 5 unit + 4 scénarios BDD + 1 TestKit → `test` **47 tests 20 failed**, puis RED ciblé healthcheck → **48 tests 1 failed**. **GREEN** : `composeBody()` corrigée (image réelle, contrat `DB_*`/`MOODLE_*`, portainer-ce sans healthcheck, workspace retiré avec TODO), `envBody()` étendu (`POSTGRES_USER`, `MOODLE_USERNAME`/`PASSWORD`/`SITENAME`). **REFACTOR** : parité structurelle conservée (`composeBody()`/`envBody()` source unique bash/batch), KDoc alignés. **Dogfooding final** : stack complet (Moodle 5.2.3 healthy + PostgreSQL + Ollama + Portainer no-healthcheck/HTTP 200) démarré depuis le compose **généré**, seed `INSERT 0 1` puis **`INSERT 0 0`** (count=1), `/login/index.php` 200. Gates : `clean check validatePlugins` VERT **21 unit + 5 functional + 15 scénarios Cucumber ×2 runners**, `publishToMavenLocal` VERT, zéro secret, zéro `mariadb`. |
| 008 | 2026-09-21 | ACADEMY-6-7 TERMINÉ + EPIC ACADEMY-6 TERMINÉ — Cucumber BDD étendu + hôte = Docker uniquement | 5 | Suite de S-007 (ordre 1 → 6 → 5 → 7). Mission : **ACADEMY-6-7** (Cucumber BDD étendu — contenu OS, compose, idempotence, erreurs, opencode.json). **Constat d'audit** : le « contenu OS » asserté contredisait D-ACADEMY-6-10 (révision pilote S-004 : « hôte = Docker uniquement, Java/Gradle vivent dans l'image workspace ») — `InstallerScriptGenerator` émettait toujours `JAVA_HOME`/`temurin`/`services.gradle.org`/`setx JAVA_HOME`/`brew install openjdk@…` (code S-003 jamais retiré). **Décision de session** : absorber ACADEMY-6-1/2/3 (raffermissement hôte) **CADUQUES** (S-008) et aligner le contenu OS sur D-6-10. **RED prouvé aux 3 niveaux** (7 unit + 6 scénarios BDD + 1 TestKit ajoutés, 2 migrés) : `InstallerScriptGeneratorTest` **46 tests 6 failed**, `cucumberTestInstaller` **42 tests 6 failed**, `functionalTest` **6 tests 1 failed**. **GREEN** : les 3 scripts n'émettent plus aucun provisioning hôte — Linux/macOS `command -v docker` sinon `exit 1` actionnable, Windows `Get-Command docker` + `if errorlevel 1 exit /b 1` (garde admin `net session` conservée) ; `JAVA_VERSION`/`GRADLE_VERSION` deviennent des **pins documentaires** de l'image workspace ; macOS Docker Desktop via Homebrew **conditionné** à l'absence de Docker, zéro `sudo`. **REFACTOR** : KDoc alignées D-6-10 (générateur + `InstallerPlatform`), helper `platform()` dupliqué consolidé. **Dogfooding** : `publishToMavenLocal` + `buildAllInstallers` sur le consommateur racine → `sh -n` VALIDE (linux+macos), `docker compose config` VALIDE, grep provisioning hôte = **0 occurrence**. Gates : `clean check validatePlugins` VERT **28 unit + 6 functional + 21 scénarios Cucumber**, `publishToMavenLocal` VERT, zéro secret, zéro `mariadb`. **EPIC ACADEMY-6 TERMINÉ** (6-0/6-4/6-5/6-6/6-7 ✅ + 6-1/2/3 CADUQUES). |
| 009 | 2026-09-21 | ACADEMY-5 TERMINÉ — exposition agent opencode (opencode.json + AGENTS.md + service workspace) | 11 | Suite de S-008 (ordre 1 → 6 → 5 → 7). Mission **ACADEMY-5** (exposition agent) + TODO S-007 (réinsérer le service `workspace`). **Gouvernance d'abord (pattern S-057)** : cadrage `CADRAGE_EPIC_ACADEMY_5_OPENCODE.adoc` (D-ACADEMY-5-1..11). **Découvertes** : (1) l'image officielle `ghcr.io/anomalyco/opencode` **existe mais est Alpine-minimal** (ni zsh/git/Java/Gradle — `docker run` réel) → inapte à exécuter les tâches Gradle ; (2) `gradle:9.7.1-jdk25` (Ubuntu 26.04, Java 25 + Gradle 9.7.1 + git + curl + apt-get) = exactement les pins D-ACADEMY-6-10 → base du Dockerfile généré. **RED prouvé** par échec de compilation (`Unresolved reference 'OpenCodeConfig'/'OpenCodeCatalog'/'OpenCodeConfigGenerator'/'LearnerGuideGenerator'/'WorkspaceDockerfileGenerator'`). **GREEN** : domaine pur `education.cccp.academy.opencode` (6 fichiers — `OpenCodeConfig`, `OpenCodePlugin`, `OpenCodeCatalog` **11 plugins publics vérifiés source S-009**, `OpenCodeConfigGenerator` JSON zéro-lib, `LearnerGuideGenerator` `AGENTS.md`, `WorkspaceDockerfileGenerator`) ; service `workspace` dans `composeBody()` (`build: ./project`, image `cccp-education/academy-workspace:latest`, `profiles: [agent]`, opt-in `docker compose --profile agent up`) ; `$APP_DIR/project/{Dockerfile,opencode.json,AGENTS.md}` écrits (source unique bash/batch → parité structurelle Linux/Windows) ; extension +3 properties (`openCodeEnabled`/`openCodeModel`/`openCodeProviderUrl`) ; runner `cucumberTestOpenCode` (pattern S-082) + glue `AcademyOpenCodeSteps` (S-088). ACADEMY-5-2 **absorbée** (le `project/` généré EST le projet consommateur). **Dogfooding réel** (leçon S-007) : exécution de l'installeur Linux dans un HOME isolé → `sh -n` VALIDE, `jq opencode.json` VALIDE, `docker compose config` VALIDE, **`docker build` du Dockerfile généré SUCCÈS**, `opencode --version` = **1.18.31** en tant que `learner`, zsh/gradle 9.7.1/java 25. **Bug attrapé par le dogfooding** (D-ACADEMY-5-11) : l'install opencode run en `root` atterrissait dans `/root/.opencode` (invisible au `learner` → `command not found`) → `USER learner` **avant** l'install + `ENV HOME=/home/learner`, prouvé par rebuild. Gates : `clean check validatePlugins` VERT **50 unit + 7 functional + 21 scénarios installer + 5 scénarios opencode**, `publishToMavenLocal` VERT, zéro secret, zéro `mariadb`. L'implémentation des ports N0 (`FormationRuntimePort`/`InteractionProtocol`) reste séquencée en ACADEMY-2/3 (exige BYOK + bridge Ktor) — D-ACADEMY-5-8. |
| 010 | 2026-09-21 | ACADEMY-7 TERMINÉ — BYOK LLM multi-provider | 14 | Suite de S-009 (TODO réel : `opencode.json` mono-provider en dur). Mission **ACADEMY-7** (BYOK). **Gouvernance d'abord (pattern S-057)** : cadrage `CADRAGE_EPIC_ACADEMY_7_BYOK.adoc` (D-ACADEMY-7-1..10). **Faits vérifiés S-010** : `runtime-contracts:0.0.1` sur Central (JAR inspecté : `ByokLlmConfig`/`LlmProviderKind`, invariants `OLLAMA_LOCAL`→baseUrl, externes→apiKeyEnvVar) ; interpolation `{env:NAME}` officielle opencode (schéma + docs) ; ids providers réels models.dev (`ollama`, `ollama-cloud` `https://ollama.com/v1`/`OLLAMA_API_KEY`, `google` npm `@ai-sdk/google`/`GEMINI_API_KEY`, `huggingface` `https://router.huggingface.co/v1`/`HF_TOKEN`) — leçon S-007 `moodle:4.5` appliquée. **RED prouvé en 2 temps** : `Unresolved reference 'contracts'` (N0 non câblé) puis `Unresolved reference 'ByokProviderCatalog'/'ByokProviderSpec'` (compilateur Kotlin = RED statique, pattern S-229), puis `'openCodeProvider'` pour le wiring. **GREEN** : domaine pur `education.cccp.academy.byok` (`ByokProviderSpec` + `ByokProviderCatalog` mapping total) ; `OpenCodeConfig` +`provider`/`apiKeyEnvVar` (défaut = sortie ACADEMY-5 byte-identique, **test de caractérisation**) ; `OpenCodeConfigGenerator` multi-provider (`baseURL` émis seulement si nécessaire, `apiKey` toujours `{env:NAME}`) ; `LearnerGuideGenerator` section BYOK concrète ; extension +2 properties (`openCodeProvider` défaut `OLLAMA_LOCAL`, `openCodeApiKeyEnvVar`) ; `.env` nom-seul + `composeBody()` transmet la variable au service `workspace` ; dépendance N0 (**supersede D-ACADEMY-5-9**). **REFACTOR** : `composeBody`/`envBody` paramétrés par la plateforme, parité Linux/Windows structurelle conservée. **Dogfooding réel** (leçon S-007) : consommateur transitoire `/tmp`, installeur exécuté en HOME isolé — GEMINI : `sh -n`/`jq`/`docker compose config` VALIDES, `.env` documente `GEMINI_API_KEY=` (vide), compose transmet `GEMINI_API_KEY: ${GEMINI_API_KEY}` ; **preuve de non-fuite** : avec `dummy-not-a-real-key`, la valeur apparaît dans le `compose config` et est **absente du `opencode.json`** ; défaut OLLAMA_LOCAL : `opencode.json` **byte-identique** ACADEMY-5. Gates : `clean check validatePlugins` VERT **218 unit + 9 functional + 21 installer + 5 opencode + 8 byok scénarios** 0 FAIL, `publishToMavenLocal` VERT, zéro secret. |
| 011 | 2026-09-21 | ACADEMY-8 TERMINÉ — webhooks Moodle + bridge Ktor | 19 | Suite de S-010. **Décision pilote 2026-09-21 : Ktor acté.** **Constats d'audit (faits vérifiés)** : (1) le « bridge opencode » existant (`SessionProtocolDaemonTask`, codebase N1) est un daemon **stdin/stdout JSON-lines**, PAS HTTP — la justification de la vision § 8 D4 était **inexacte**, Ktor est un transport **additionnel** (D-ACADEMY-8-1) ; (2) **Moodle n'a aucun webhook natif** (plugin tiers `local_webhooks` absent de l'image, payload non vérifiable — Cloudflare) → schéma d'événement **propre à academy** (D-ACADEMY-8-5, leçon S-007) ; (3) Ktor 3.2.0 sur Central (core/netty/content-negotiation/serialization/test-host) ; (4) `opencode-session-contracts` **non transitif** (RED n°1) ; (5) les contrats N0 **ne sont pas `@Serializable`** (RED de comportement en GREEN : `Serializer for class 'SessionResponse' is not found`) → **anti-corruption layer** `BridgeWire` (le contrat partagé reste pur). **Gouvernance d'abord (pattern S-057)** : cadrage `CADRAGE_EPIC_ACADEMY_8_KTOR.adoc` (D-ACADEMY-8-1..10). **RED prouvé en 3 temps** par échec de compilation (dépendance → domaine → wiring) **+ 1 RED de comportement** (sérialisation). **GREEN** : domaine pur `education.cccp.academy.bridge` (`MoodleEvent` payload opaque, `BridgeEventTranslator` → `SessionPrompt` N0 à **session id stable** via `UUID.nameUUIDFromBytes` — rejouable, `BridgeRoutes` table unique, `BridgeServerConfig`, `BridgeWire`, `ByokBridgeSupport`) + serveur `WebhookBridgeServer` (`installBridge` module unique test+prod, `startBridge` Netty daemon, acks `BridgeAck`/`BridgeHealth`, 400/404) + tâche `ServeWebhookBridgeTask` **opt-in** (`@DisableCachingByDefault`, `onlyIf { bridgeEnabled }`, seam `serverLauncher` pattern codex `storeOverride`) + extension +3 properties (`bridgeEnabled` défaut `false`, `bridgeHost` `127.0.0.1`, `bridgePort` `8765`) + `AGENTS.md` section bridge **conditionnelle** (`BridgeGuide`). Le bridge **route et acquitte — il ne pense pas** (zéro LLM, D-ACADEMY-8-6). **Dogfooding réel** (leçon S-007) : consommateur transitoire /tmp, serveur lancé par la tâche Gradle port 8791, `curl` réel → `GET /health` 200 `{"status":"up","provider":"ollama","model":"gpt-oss:120b-cloud"}`, `POST /events/moodle` ack `COMPLETED` session stable, `POST /session` ack, JSON invalide **400**, route inconnue **404**, arrêt propre. Gates : `clean check validatePlugins` VERT **311 unit + 10 functional + 21 installer + 5 opencode + 8 byok + 6 bridge scénarios** 0 FAIL, `publishToMavenLocal` VERT, tâche SKIPPED par défaut (TestKit), zéro secret. |
| 012 | 2026-09-21 | ACADEMY-4 TERMINÉ — consommation du matériel versionné + publication Maven Central | 19 | Suite de S-011. **Constat central (audit S-012, faits vérifiés)** : la formulation du BACKLOG était **inexacte sur deux points** — (1) `MaterialUpdateContract` est une **data class** (pas une interface : le resolver est `MaterialUpdateResolver`) ; (2) le **transport git appartient au bureau** (vision § 3.2 : « bureau l'implémente, academy le lit »), et `pullMaterial` **n'existe pas encore** (`bureau-gradle` = 3 fichiers, zéro `tasks.register`, mention KDoc seulement → jamais cité comme exécutable, leçons S-007/S-011). **Gouvernance d'abord (pattern S-057)** : cadrage `CADRAGE_EPIC_ACADEMY_4_MATERIAL.adoc` (9 constats, D-ACADEMY-4-1..12). **RED prouvé** par échec de compilation (domaine puis wiring). **GREEN** : domaine pur `education.cccp.academy.material` (`MaterialArtifactType` 6 livrables + `fromPivotType` qui ne devine jamais, `MaterialRequirement` = lecture N0, `MaterialManifest` = lecture du **pivot EPIC K** `metadata.json` tolérante `ignoreUnknownKeys` + JSON malformé → null, `MaterialReadiness` sealed `Ready`/`Incomplete`/`Empty`, `MaterialInspector` verdict pur sans exception, `MaterialGuideGenerator`) + tâche `InspectTrainingMaterialTask` (thin-wrapper : n'échoue **que** si exigence déclarée non satisfaite ; `Empty` succès — la ressource est générée avant le pull) + extension +4 properties + `InstallerPlatform.material` + scaffold `MATERIAL.md` **conditionnel** (source unique bash/batch, parité structurelle). **Frontière vérifiable mécaniquement** : grep `ProcessBuilder`/git dans le code prod = **0**. **Découverte du dogfooding** (2e fois que le réel paie) : `materialExpectedTypes` renseigné **sans** remote était traité comme « pas d'exigence » → silencieusement ignoré ; correctif TDD `hasRequirement()`. **Dogfooding réel** : consommateur transitoire /tmp + pivots EPIC K réels → **READY** (SPG+SLIDES), **FAIL** actionnable (`outdated (expected version v2.0)`), **FAIL** (`missing QUIZ`), **SUCCESS** `nothing to verify` (backward compat), `MATERIAL.md` généré + `sh -n` VALIDE + `grep pullMaterial` = 0. **Publication Maven Central** (demande pilote) : pré-requis vérifiés (deps releases HTTP 200), nmcp 1.5.0 ajouté aux settings, `nmcpCheckAggregationFiles` VERT, bundle **`5c54af3f-1bfc-405f-950a-1172fff86b7a`** AUTOMATIC, **propagation HTTP 200** (academy-plugin + marker `education.cccp.academy.gradle.plugin`). **EPIC ACADEMY-10 cadré** (migration Ktor → Spring Boot WebFlux + coroutines + R2DBC + PostgreSQL Docker + Jackson 3, référence `sources/draft/api`) — faits vérifiés : Boot 4.1.1 = Jackson 3.1.5 par défaut, Boot 3.4.2 (BOM workspace) = Jackson 2.18.2 → bump BOM probable. Gates : `clean check validatePlugins` VERT **152 unit + 13 functional + 21 installer + 5 opencode + 8 byok + 6 bridge + 8 material scénarios** 0 FAIL, `publishToMavenLocal` VERT, zéro secret. |
| 013 | 2026-09-21 | ACADEMY-11 3/4 US — injection du matériel dans Moodle + frontière runtime actée + ToolExposure | 19 | **Mission recadrée par l'arbitrage pilote** : « academy aide à consommer la formation générée par training et installe le runtime Docker ; j'injecte de la **data structurée** pour être intégrée dans Moodle » — puis « on s'appuie sur l'**image préconstruite** et ses **outils natifs** ». **Constats d'audit (faits vérifiés)** : (1) la frontière `FormationRuntimePort` est tranchée par trois sources concordantes (`VISION_FORMATION_INTERACTIVE` § D3 + itération 9, KDoc `BureauPlugin.kt:24`, règle licence 3 étages) — l'orchestration agentique est **privée**, `VISION_PIPELINE_FORMATION` § 6 est contredit ; (2) le TODO « `SessionMemoryContract` zéro implémenteur » est **périmé** — `codex/profile/PgVectorSessionMemory.kt:37` l'implémente depuis S-094 ; (3) `ToolExposure` (N0) est orphelin (jar `runtime-contracts:0.0.1`, seule mention prod = une KDoc). **Découverte décisive** : l'image `erseco/alpine-moodle:v5.2.3` **embarque des outils natifs** — `moodle-blueprint` (format Moodle Playground, appliqué auto au boot via `MOODLE_BLUEPRINT` ; `createSection`/`addModule` = PLANNED) et **`moosh`** (Moodle Shell : `course-create -r` idempotent, `activity-add`, `section-config-set`). **Gouvernance d'abord (pattern S-057)** : cadrage `CADRAGE_EPIC_ACADEMY_11_MOODLE.adoc` (D-ACADEMY-11-1..10). **Pivot de design en cours de session** : un premier applicateur PHP maison (réimplémentant `create_course`/`add_moduleinfo`) a été **abandonné** au profit d'un applicateur shell pilotant les outils natifs — **zéro PHP maison, zéro SQL, zéro DDL, zéro stack**. **RED prouvé** par échec de compilation à chaque palier (exposure, domaine, générateur, tâche). **GREEN** : domaine pur `education.cccp.academy.moodle` (`MoodleActivityType`/`MoodleActivityMapping` vocabulaire Moodle **réel**, `MoodleMaterial` structure+pivot **path-only**, `MoodleActivity`/`MoodleSection`, `MoodleImportPlan`, `MoodlePlanBuilder`, `MoodleIngestionScriptGenerator`) + tâche `generateMoodleImport` (thin-wrapper, `onlyIf` opt-in, `@InputDirectory @Optional`, dégradée : matériel vide → **rien écrit**, installer byte-identique) + extension +4 properties + scaffold `plan.json`/`ingest.sh` + runner `cucumberTestMoodle` (S-082) + glue préfixée (S-088). **ToolExposure branché** : `OpenCodeCatalog.PUBLIC_BOROUGHS_ROOT` + `exposure()` lit le contrat N0, le guide en dérive le guardrail (fin du triple énoncé). **Boundary § 3.3 respecté** : le `.adoc` est un payload opaque référencé par chemin, lu **Moodle-side** (`cat`). **Dogfooding réel** (leçon S-007, 3e fois que le réel paie) : deux runs Moodle 5.2.3 + PostgreSQL 16 via l'image préconstruite — test des outils natifs puis **application réelle du script généré** → cours `formation-fpa`, sections `01_accueil`/`02_coeur`, activités `label`/`quiz`/`page`, contenu de page réel. **3 bugs réels attrapés** : (a) `SHORTNAME=""formation-fpa""` double-guillemet (shell invalide), (b) `awk -F'"' '$4'` mauvais champ du CSV `moosh course-list` (le shortname est en position 3), (c) `label` attend `--intro` mais `page` attend `--content --contentformat`. Chacun verrouillé par un test de régression. Gates : `clean check validatePlugins` VERT **183 unit + 13 functional + 59 scénarios Cucumber** (21 installer + 6 opencode + 8 byok + 6 bridge + 8 material + 10 moodle) 0 FAIL, `publishToMavenLocal` VERT, zéro secret. **Reste ACADEMY-11-4** (service compose one-shot `moodle-material`, non livré faute de preuve `compose up`). |
| 014 | 2026-09-25 | ACADEMY-12 cadré + ACADEMY-12-1 TERMINÉ — provisionnement engine conteneurs par OS + déblocage build | 8 | **Demande pilote recadrée par les faits** : tester l'installation Windows via le runner GitHub → **écarté** (les runners ne fournissent que des conteneurs **Windows** ; Moodle/PostgreSQL/Ollama/Portainer sont des images Linux, aucune preuve d'installation réelle) ; « Docker Engine sans Desktop » → **impossible** sur Windows pour les images Linux (`docs.docker.com/engine/install/` : Engine natif = Linux) et « sans WSL » est impossible (Docker Desktop **gère** sa distro WSL2, activation admin unique + reboot possible) ; sur macOS « Engine sans Desktop » est **impossible** (VM obligatoire) → **Colima** (MIT). **Matrice d'engine actée (arbitrage pilote)** : Linux = Docker Engine natif ; Windows = Docker Desktop (`install --user --quiet --accept-license --backend=wsl-2`) ; macOS = Colima ; **Portainer partout** (uniformité apprenant, même URL `:9000`) — **supersede D-ACADEMY-6-12** (« Portainer remplace Docker Desktop » = erreur de catégorie : Portainer est une GUI, pas un moteur). **Gouvernance d'abord (pattern S-057)** : cadrage `CADRAGE_EPIC_ACADEMY_12_WINDOWS.adoc` (8 constats, D-ACADEMY-12-1..8, options écartées). **Décision pure / exécution adapter** : domaine `academy.env` sans I/O ; la sonde (OS/admin/WSL2/daemon) appartient à l'adapter. **Préalable P0** : `settings.gradle.kts:23` faisait `error("ossrhUsername not found")` → `./gradlew` échouait sur un hôte nu (prouvé `help -Duser.home=/tmp/faux` → BUILD FAILED ; **les 25 boroughs** partagent ce pattern, zéro garde) → nmcp appliqué **seulement si** les 2 credentials sont présents. **RED prouvé** : 30 `Unresolved reference` (unit) + 7 scénarios BDD `UndefinedStepException` (preuve par retrait temporaire de la glue). **GREEN** : domaine pur `education.cccp.academy.env` (`HostOs`, `EnvironmentFacts`, `DockerStrategy` sealed — `AlreadyReady`/`InstallDockerEngine`/`InstallColima`/`InstallDockerDesktop`/`BootstrapWsl2First`/`Unsupported`, `DockerStrategyDecider` pur) + BDD `academy_env.feature` 7 scénarios `@env` + `AcademyEnvSteps` (S-088) + `AcademyEnvCucumberRunner` (S-082) + `cucumberTestEnv` câblé à `check`. **Contrainte reboot assumée** (D-ACADEMY-12-4) : installateur idempotent/resumable, jamais promis « sans reboot ». Gates : `clean check validatePlugins` VERT **193 unit (+10) + 13 functional + 68 scénarios Cucumber** (21 installer + 6 opencode + 8 byok + 6 bridge + 8 material + 10 moodle + **7 env**) 0 FAIL, `publishToMavenLocal` VERT, `help -Duser.home=<vide>` VERT (P0), zéro secret, zéro `ProcessBuilder` dans le domaine. **Reste ACADEMY-12-2** (writers + sonde adapter) et **ACADEMY-12-3** (dogfooding Windows réel). |
|===